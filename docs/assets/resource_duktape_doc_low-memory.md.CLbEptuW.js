import{_ as o,c as t,a2 as i,o as n}from"./chunks/framework.DzmM640o.js";const m=JSON.parse('{"title":"Low memory environments","description":"","frontmatter":{},"headers":[],"relativePath":"resource/duktape/doc/low-memory.md","filePath":"resource/duktape/doc/low-memory.md","lastUpdated":1732350347000}'),a={name:"resource/duktape/doc/low-memory.md"};function l(s,e,r,c,u,p){return n(),t("div",null,e[0]||(e[0]=[i(`<h1 id="low-memory-environments" tabindex="-1">Low memory environments <a class="header-anchor" href="#low-memory-environments" aria-label="Permalink to &quot;Low memory environments&quot;">​</a></h1><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>This document describes suggested feature options for reducing Duktape memory usage for memory-constrained environments, which are one important portability target for Duktape.</p><p>The default Duktape options are quite memory conservative, and significant ECMAScript programs can be executed with e.g. 1MB of memory. Currently realistic memory targets are roughly:</p><ul><li>256-384kB system flash memory (code) and 256kB system RAM <ul><li>Duktape compiled with default options is feasible</li><li>Duktape compiles to around 200-210kB of code (x86), so 256kB is technically feasible but leaves little space for user bindings, hardware initialization, communications, etc; 384kB is a more realistic flash target</li></ul></li><li>256-384kB system flash memory (code) and 128kB system RAM <ul><li>Duktape feature options are needed to reduce memory usage</li><li>A custom pool-based memory allocation with manually tuned pools may be required</li><li>Aggressive measures like lightweight functions, 16-bit fields for various internal structures (strings, buffers, objects), pointer compression, external strings, etc may need to be used</li></ul></li><li>192-256kB system flash memory (code) and 96kB system RAM <ul><li>Requires a bare metal system, possibly a custom C library, etc.</li><li><a href="http://pt.slideshare.net/seoyounghwang77/js-onmicrocontrollers" target="_blank" rel="noreferrer">http://pt.slideshare.net/seoyounghwang77/js-onmicrocontrollers</a></li></ul></li><li>160-192kB system flash memory (code) and 64kB system RAM <ul><li>Requires a bare metal system, possibly a custom C library, etc.</li><li>Requires use of ROM strings and objects to reduce Duktape startup RAM usage (which drops to around 2-3kB with ROM strings/objects).</li></ul></li><li>128kB system flash memory (code) and 32kB system RAM <ul><li>Requires the above, and removing built-in bindings like the global Object (DUK_USE_OBJECT_BUILTIN), Array (DUK_USE_ARRAY_BUILTIN), etc bindings. See stripped configuration examples, and add back whatever bindings are absolutely necessary.</li></ul></li></ul><p>There are four basic goals for low memory optimization:</p><ol><li>Reduce Duktape code (flash) footprint. This is currently a low priority item because flash size doesn&#39;t seem to be a bottleneck for most users. Techniques includes dropping optional Duktape functionality and compiler options.</li><li>Reduce initial memory usage of a Duktape heap. This provides a baseline for memory usage which won&#39;t be available for user code (technically some memory can be reclaimed by deleting some built-ins after heap creation). Techniques include pointer compression, external strings, ROM-based strings and objects, and reducing built-in object and property count.</li><li>Minimize the growth of the Duktape heap relative to the scope and complexity of user code, so that as large programs as possible can be compiled and executed in a given space. Important contributing factors include the footprint of user-defined ECMAScript and Duktape/C functions, the size of compiled bytecode, etc. Techniques include reducing object and property count for e.g. function objects.</li><li>Make remaining memory allocations as friendly as possible for the memory allocator, especially a pool-based memory allocator. Concretely, prefer small chunks over large contiguous allocations.</li></ol><p>The following genconfig option file template enables most low memory related option: <code>config/examples/low_memory.yaml</code>. It doesn&#39;t enable pointer compression because that always requires some application specific code. More aggressive feature stripping examples are in <code>config/examples/low_memory_strip.yaml</code>.</p><h2 id="optimizing-code-footprint" tabindex="-1">Optimizing code footprint <a class="header-anchor" href="#optimizing-code-footprint" aria-label="Permalink to &quot;Optimizing code footprint&quot;">​</a></h2><p>The best options for reducing code footprint depend obviously on your compiler. The footprint difference between different options can be quite large, sometimes over 20%, so for targets with code footprint limitations it&#39;s worth it to investigate the best compiler specific options.</p><h3 id="gcc-size-optimization" tabindex="-1">Gcc size optimization <a class="header-anchor" href="#gcc-size-optimization" aria-label="Permalink to &quot;Gcc size optimization&quot;">​</a></h3><p>Based on: <a href="https://software.intel.com/en-us/blogs/2013/01/17/x86-gcc-code-size-optimizations" target="_blank" rel="noreferrer">https://software.intel.com/en-us/blogs/2013/01/17/x86-gcc-code-size-optimizations</a>.</p><p>Default <code>Makefile.cmdline</code> on x64 which uses <code>-O2 -fomit-frame-pointer</code>:</p><pre><code>text     data      bss      dec      hex  filename
231549     1184       56   232789    38d55  duk
</code></pre><p>Adding <code>-flto</code>:</p><pre><code>text     data      bss      dec      hex  filename
219825     1160       56   221041    35f71  duk
</code></pre><p>Adding <code>-flto -fno-asynchronous-unwind-tables</code>:</p><pre><code>text     data      bss      dec      hex  filename
186745     1160       56   187961    2de39  duk
</code></pre><p>Adding <code>-flto -fno-asynchronous-unwind-tables -ffunction-sections -Wl,--gc-sections</code>:</p><pre><code>text     data      bss      dec      hex  filename
186666     1144       56   187866    2ddda  duk
</code></pre><p>Adding an explicit <code>-fno-stack-protector -fno-stack-check</code> may also have an effect.</p><h3 id="stripping-unused-api-functions" tabindex="-1">Stripping unused API functions <a class="header-anchor" href="#stripping-unused-api-functions" aria-label="Permalink to &quot;Stripping unused API functions&quot;">​</a></h3><p>If you compile and link your application and Duktape statically, you can often strip away any Duktape API functions which are not actually used by your application or Duktape. Doing so requires compiler specific steps, but see for example:</p><ul><li><a href="http://stackoverflow.com/questions/6687630/how-to-remove-unused-c-c-symbols-with-gcc-and-ld" target="_blank" rel="noreferrer">http://stackoverflow.com/questions/6687630/how-to-remove-unused-c-c-symbols-with-gcc-and-ld</a></li></ul><p>Example using GCC: compile the Duktape command line utility without removing unused API symbols:</p><pre><code>$ gcc -o duk -Os -pedantic -std=c99 -Wall -fstrict-aliasing \\
      -fomit-frame-pointer -I./src src/duktape.c \\
      examples/cmdline/duk_cmdline.c -lm
$ size duk
   text     data      bss      dec      hex  filename
 231079     1184       56   232319    38b7f  duk
</code></pre><p>Add GCC specific options to remove unused symbols:</p><pre><code># With -fdata-sections -ffunction-sections -Wl,--gc-sections:
$ gcc -o duk -Os -pedantic -std=c99 -Wall -fstrict-aliasing \\
      -fomit-frame-pointer -I./src -fdata-sections \\
      -ffunction-sections -Wl,--gc-sections \\
      src/duktape.c examples/cmdline/duk_cmdline.c -lm
$ size duk
   text     data      bss      dec      hex  filename
 222743     1152       48   223943    36ac7  duk
</code></pre><p>Here the difference is ~8kB on x64. For the dist Makefile.hello example, which uses very few public API calls, the difference is ~12kB.</p><h3 id="miscellaneous" tabindex="-1">Miscellaneous <a class="header-anchor" href="#miscellaneous" aria-label="Permalink to &quot;Miscellaneous&quot;">​</a></h3><ul><li>On some low memory targets only libc features which are actually used get pulled into the binary being built. In such cases it may be useful to avoid calling platform sprintf/sscanf because they may be surprisingly large (&gt;20 kB). You can use <code>extras/minimal-printf</code> instead.</li><li>Math functions can sometimes have a relatively large footprint (15-20 kB), usually from trigonometric and other transcendental functions. You can stub out unnecessary functions in <code>duk_config.h</code>. Note, however, that Duktape internals at present depend on a few Math functions like <code>DUK_FMOD()</code>.</li></ul><h2 id="suggested-options" tabindex="-1">Suggested options <a class="header-anchor" href="#suggested-options" aria-label="Permalink to &quot;Suggested options&quot;">​</a></h2><p>NOTE: This list is not exhaustive, see <code>config/examples/low_memory.yaml</code>.</p><ul><li>Use the default memory management settings: although reference counting increases heap header size, it also reduces memory usage fluctuation which is often more important than absolute footprint.</li><li>By default Duktape uses natural alignment, which in practice translates to <code>DUK_USE_ALIGN_BY=8</code>. If the target can use lower alignment, consider forcing alignment to 4 or 1.</li><li>If the target has a shallow C stack, you may want to limit C stack recursion, see: <ul><li><code>config/examples/shallow_c_stack.yaml</code></li></ul></li><li>Reduce error handling footprint with one or more of: <ul><li><code>#undef DUK_USE_AUGMENT_ERRORS</code></li><li><code>#undef DUK_USE_TRACEBACKS</code></li><li><code>#undef DUK_USE_VERBOSE_ERRORS</code></li><li><code>#undef DUK_USE_VERBOSE_EXECUTOR_ERRORS</code></li><li><code>#undef DUK_USE_PC2LINE</code></li></ul></li><li>Use slower but more compact lexer algorithm (saves on code footprint): <ul><li><code>#undef DUK_USE_LEXER_SLIDING_WINDOW</code></li></ul></li><li>Disable JSON fast paths (saves on code footprint): <ul><li><code>#undef DUK_USE_JSON_STRINGIFY_FASTPATH</code></li><li><code>#undef DUK_USE_JSON_QUOTESTRING_FASTPATH</code></li><li><code>#undef DUK_USE_JSON_DECSTRING_FASTPATH</code></li><li><code>#undef DUK_USE_JSON_DECNUMBER_FASTPATH</code></li><li><code>#undef DUK_USE_JSON_EATWHITE_FASTPATH</code></li></ul></li><li>If you don&#39;t need Node.js Buffer and ES2015 typed array support, use: <ul><li><code>#undef DUK_USE_BUFFEROBJECT_SUPPORT</code></li></ul></li><li>If you don&#39;t need the Duktape-specific additional JX/JC formats, use: <ul><li><code>#undef DUK_USE_JX</code></li><li><code>#undef DUK_USE_JC</code></li></ul></li><li>Features borrowed from ECMAScript ES2015 can usually be disabled (not exhaustive): <ul><li><code>#undef DUK_USE_ES6_OBJECT_SETPROTOTYPEOF</code></li><li><code>#undef DUK_USE_ES6_OBJECT_PROTO_PROPERTY</code></li><li><code>#undef DUK_USE_ES6_PROXY</code></li></ul></li><li>If you don&#39;t need regexp support, use: <ul><li><code>#undef DUK_USE_REGEXP_SUPPORT</code></li></ul></li><li>Disable unnecessary parts of the C API: <ul><li><code>#undef DUK_USE_BYTECODE_DUMP_SUPPORT</code></li></ul></li><li>Duktape debug code uses a large, static temporary buffer for formatting debug log lines. If you&#39;re running with debugging enabled, use e.g. the following to reduce this overhead: <ul><li><code>#define DUK_USE_DEBUG_BUFSIZE 2048</code></li></ul></li><li>If strict Unicode support is not critical in your application, you can: <ul><li>Strip the <code>UnicodeData.txt</code> and <code>SpecialCasing.txt</code> files manually. There are example files in the distributable for Unicode data limited to 8-bit codepoints.</li><li>Provide the stripped files to <code>configure.py</code> to reduce Unicode table size.</li><li>Possible footprint savings are about 2-3kB.</li></ul></li></ul><h2 id="more-aggressive-options" tabindex="-1">More aggressive options <a class="header-anchor" href="#more-aggressive-options" aria-label="Permalink to &quot;More aggressive options&quot;">​</a></h2><p>The following may be needed for very low memory environments (e.g. 96-128kB system RAM):</p><ul><li>Consider using lightweight functions for your Duktape/C bindings and to force Duktape built-ins to be lightweight functions: <ul><li><code>#define DUK_USE_LIGHTFUNC_BUILTINS</code></li></ul></li><li>If code footprint is a significant issue, disabling reference counting reduces code footprint by several kilobytes at the cost of more RAM fluctuation: <ul><li><code>#undef DUK_USE_REFERENCE_COUNTING</code></li><li><code>#undef DUK_USE_DOUBLE_LINKED_LIST</code></li></ul></li><li>Enable other 16-bit fields to reduce header size; these are typically used together (all or none): <ul><li><code>#define DUK_USE_REFCOUNT16</code> (and <code>#undef DUK_USE_REFCOUNT32</code>)</li><li><code>#define DUK_USE_STRHASH16</code></li><li><code>#define DUK_USE_STRLEN16</code></li><li><code>#define DUK_USE_BUFLEN16</code></li><li><code>#define DUK_USE_OBJSIZES16</code></li><li><code>#undef DUK_USE_HSTRING_CLEN</code></li></ul></li><li>Enable heap pointer compression, assuming pointers provided by your allocator can be packed into 16 bits: <ul><li><code>#define DUK_USE_HEAPPTR16</code></li><li><code>#define DUK_USE_HEAPPTR_ENC16(udata,p) ...</code></li><li><code>#define DUK_USE_HEAPPTR_DEC16(udata,x) ...</code></li><li>Note: you cannot currently enable Duktape debug prints (DUK_USE_DEBUG) when heap pointer compression is enabled.</li></ul></li><li>Enable data pointer compression if possible. Note that these pointers can point to arbitrary memory locations (outside Duktape heap) so this may not be possible even if Duktape heap pointers can be compressed: <ul><li><code>#define DUK_USE_DATAPTR16</code></li><li><code>#define DUK_USE_DATAPTR_ENC16(udata,p) ...</code></li><li><code>#define DUK_USE_DATAPTR_DEC16(udata,x) ...</code></li><li><strong>UNIMPLEMENTED AT THE MOMENT</strong></li></ul></li><li>Enable C function pointer compression if possible. Duktape compiles to around 200kB of code, so assuming an alignment of 4 this may only be possible if there is less than 56kB of user code: <ul><li><code>#define DUK_USE_FUNCPTR16</code></li><li><code>#define DUK_USE_FUNCPTR_ENC16(udata,p) ...</code></li><li><code>#define DUK_USE_FUNCPTR_DEC16(udata,x) ...</code></li><li><strong>UNIMPLEMENTED AT THE MOMENT</strong></li></ul></li><li>Configure string table parameters. Often in low memory targets it&#39;s preferable to use a fixed size, i.e. set the minimum and maximum size to the same value. For example: <ul><li><code>#define DUK_USE_STRTAB_MINSIZE 128</code></li><li><code>#define DUK_USE_STRTAB_MAXSIZE 128</code></li><li><code>#define DUK_USE_STRTAB_PTRCOMP</code></li></ul></li><li>Use &quot;external&quot; strings to allocate most strings from flash (there are multiple strategies for this, see separate section): <ul><li><code>#define DUK_USE_EXTERNAL_STRINGS</code></li><li><code>#define DUK_USE_EXTSTR_INTERN_CHECK(udata,ptr,len) ...</code></li><li><code>#define DUK_USE_EXTSTR_FREE(udata,ptr) ...</code></li><li>As of Duktape 1.5 an alternative to external strings is to move strings (including the string heap header) to ROM, see below.</li></ul></li><li>Enable struct packing in compiler options if your platform doesn&#39;t have strict alignment requirements, e.g. on gcc/x86 you can: <ul><li><code>-fpack-struct=1</code> or <code>-fpack-struct=2</code></li></ul></li></ul><p>The following may be appropriate when even less memory is available (e.g. 64kB system RAM):</p><ul><li>Consider moving built-in strings and objects into ROM (a read-only data section): <ul><li><code>DUK_USE_ROM_STRINGS</code> and <code>DUK_USE_ROM_OBJECTS</code> (define both). See: <code>config/examples/rom_builtins.yaml</code>.</li><li><code>DUK_USE_ROM_GLOBAL_CLONE</code> or <code>DUK_USE_ROM_GLOBAL_INHERIT</code> if a writable global object is needed. <code>DUK_USE_ROM_GLOBAL_INHERIT</code> is more memory efficient: it creates a writable (empty) global object which inherits from the ROM global object.</li><li>Rerun <code>configure.py</code> with <code>--rom-support</code> to create prepared sources with support for ROM builtins. ROM builtin support is not enabled by default because it increases the size of <code>duktape.c</code> considerably. Add the option <code>--rom-auto-lightfunc</code> to convert built-in function properties into lightfuncs to reduce ROM footprint. (See repo Makefile <code>duk-low-rom</code> target for some very simple examples.)</li><li>Moving built-ins into ROM makes them read-only which has some side effects. Some side effects are technical compliance issues while others have practical impact and may prevent running some existing scripts. The following testcases illustrate some of the issues: <ul><li><code>tests/ecmascript/test-dev-rom-builtins-1.js</code></li><li><code>tests/api/test-dev-rom-builtins-1.c</code></li></ul></li><li>When using pointer compression you need to add support for compressing ROM strings, see <code>doc/objects-in-code-section.rst</code> and a concrete example in <code>examples/cmdline/duk_cmdline_lowmem.c</code>.</li><li>See <code>doc/objects-in-code-section.rst</code> for technical details and current limitations.</li></ul></li><li>Consider also moving your own built-in objects and strings into ROM: <ul><li>User strings and objects can also be moved into ROM. You can also modify default Duktape built-ins, adding and removing properties, etc. For more details, see: <ul><li><code>util/example_user_builtins1.yaml</code>: examples of user builtins</li><li><code>src-input/builtins.yaml</code>: documents some more format details</li><li>Repo Makefile <code>duk-low-rom</code> target: illustrates how to run <code>configure.py</code> with user builtins</li></ul></li></ul></li><li>Consider using lightfuncs for representing function properties of ROM built-ins. <ul><li>For custom built-ins you can use a &quot;lightfunc&quot; type for a property value directly.</li><li>You can also request automatic lightfunc conversion for built-in function properties using <code>--rom-auto-lightfunc</code>. This saves around 15kB for Duktape built-ins.</li></ul></li></ul><h2 id="notes-on-pointer-compression" tabindex="-1">Notes on pointer compression <a class="header-anchor" href="#notes-on-pointer-compression" aria-label="Permalink to &quot;Notes on pointer compression&quot;">​</a></h2><p>Pointer compression can be applied throughout (where it matters) for three pointer types:</p><ul><li>Compressed 16-bit Duktape heap pointers, assuming Duktape heap pointers can fit into 16 bits, e.g. max 256kB memory pool with 4-byte alignment</li><li>Compressed 16-bit function pointers, assuming C function pointers can fit into 16 bits</li><li>Compressed 16-bit non-Duktape-heap data pointers, assuming C data pointers can fit into 16 bits</li></ul><p>Pointer compression can be quite slow because often memory mappings are not linear, so the required operations are not trivial. NULL also needs specific handling.</p><p>When ROM object/string support is enabled, pointer compression and decompression must support ROM pointer compression. This is done by reserving a range of 16-bit compressed pointer values to represent ROM pointers, and to use a ROM pointer table to compress/decompress ROM pointers. See <code>examples/cmdline/duk_cmdline_lowmem.c</code> for an example.</p><h2 id="external-string-strategies-duk-use-extstr-intern-check" tabindex="-1">External string strategies (DUK_USE_EXTSTR_INTERN_CHECK) <a class="header-anchor" href="#external-string-strategies-duk-use-extstr-intern-check" aria-label="Permalink to &quot;External string strategies (DUK_USE_EXTSTR_INTERN_CHECK)&quot;">​</a></h2><p>The feature can be used in two basic ways:</p><ul><li>You can anticipate a set of common strings, perhaps extracted by parsing source code, and build them statically into your program. The strings will then be available in the &quot;text&quot; section of your program. This works well if the set of common strings can be estimated well, e.g. if the program code you will run is mostly known in advance.</li><li>You can write strings to memory mapped flash when the hook is called. This is less portable but can be effective when the program you will run is not known in advance.</li></ul><p>Note that:</p><ul><li>Using an external string pointer for short strings (e.g. 3 chars or less) is counterproductive because the external pointer takes more room than the character data.</li></ul><p>The Duktape built-in strings are available from prepared source metadata:</p><ul><li>For example, <code>dist/src/duk_source_meta.json</code>, the <code>builtin_strings_base64</code> contains the byte exact strings used, encoded with base-64.</li></ul><p>Strings used by application C and ECMAScript code can be extracted with various methods. The Duktape main repo contains an example script for scraping strings from C and ECMAScript code using regexps:</p><ul><li><code>util/scan_strings.py</code></li></ul><p>There are concrete examples for some external string strategies in:</p><ul><li><code>dist/examples/cmdline/duk_cmdline_lowmem.c</code></li></ul><h2 id="tuning-pool-sizes-for-a-pool-based-memory-allocator" tabindex="-1">Tuning pool sizes for a pool-based memory allocator <a class="header-anchor" href="#tuning-pool-sizes-for-a-pool-based-memory-allocator" aria-label="Permalink to &quot;Tuning pool sizes for a pool-based memory allocator&quot;">​</a></h2><p>The memory allocations used by Duktape depend on the architecture and especially the low memory options used. So, the safest approach is to select the options you want to use and then measure actual allocation patterns of various programs.</p><p>The memory allocations needed by Duktape fall into two basic categories:</p><ul><li>A lot of small allocations (roughly between 16 and 128 bytes) are needed for strings, buffers, objects, object property tables, etc. These allocation sizes constitute most of the allocation activity, i.e. allocs, reallocs, and frees. There&#39;s a lot churn (memory being allocated and freed) even when memory usage is nearly constant.</li><li>Much fewer larger allocations with much less activity are needed for ECMAScript function bytecode, large strings and buffers, value stacks, the global string table, and the Duktape heap object.</li></ul><p>The <code>examples/alloc-logging</code> memory allocator can be used to write out an allocation log file. The log file contains every alloc, realloc, and free, and will record both new and old sizes for realloc. This allows you to replay the allocation sequence so that you can simulate the behavior of pool sizes offline.</p><p>The <code>examples/allog-logging/pool_simulator.py</code> simulates pool allocator behavior for a given allocation log, and provides a lot of detailed graphs of pool usage, allocated bytes, waste bytes, etc. It also provides some tools to optimize pool counts for one or multiple application &quot;profiles&quot;. See detailed description below.</p><p>You can also get a dump of Duktape&#39;s internal struct sizes by enabling debug prints; Duktape will debug print struct sizes when a heap is created. The struct sizes will give away the minimum size needed by strings, buffers, objects, etc. They will also give you <code>sizeof(duk_heap)</code> which is a large allocation that you should handle explicitly in pool tuning.</p><p>Finally, you can look at existing projects and what kind of pool tuning they do.</p><h2 id="tuning-pool-sizes-using-pool-simulator-py" tabindex="-1">Tuning pool sizes using pool_simulator.py <a class="header-anchor" href="#tuning-pool-sizes-using-pool-simulator-py" aria-label="Permalink to &quot;Tuning pool sizes using pool_simulator.py&quot;">​</a></h2><h3 id="overview-1" tabindex="-1">Overview <a class="header-anchor" href="#overview-1" aria-label="Permalink to &quot;Overview&quot;">​</a></h3><p>The pool simulator replays allocation logs, simulates the behavior of a pool-based memory allocator, and provides several useful commands:</p><ul><li>Replay an allocation log and provide statistics and graphs for the pool performance: used bytes, wasted bytes, by-pool breakdowns, etc.</li><li>Optimize pool counts based on a high-water-mark measurement, when given pool byte sizes (a base pool configuration) and an allocation log.</li><li>Optimize pool counts based on a more complex algorithm which takes pool borrowing into account (see discussion below).</li><li>Generate a pool configuration for a given total memory target, given the tight pool configuration for Duktape and a set of representative applications.</li></ul><p>These operations are discussed in more detail below.</p><h3 id="important-notes" tabindex="-1">Important notes <a class="header-anchor" href="#important-notes" aria-label="Permalink to &quot;Important notes&quot;">​</a></h3><ul><li>Before optimizing pools, you should select Duktape feature options (especially low memory options) carefully.</li><li>It may be useful to use DUK_USE_GC_TORTURE to ensure that there is no slack in memory allocations; reference counting frees unreachable values but does not handle loops. When GC torture is enabled, Duktape will run a mark-and-sweep for every memory allocation. High-water-mark values will then reflect the memory usage achievable in an emergency garbage collect.</li><li>The pool simulator provides quite simple pool allocator behavior. If your pool allocator has different basic features (for example, splitting and merging of chunks) you&#39;ll need to tweak the pool simulator to get useful results.</li></ul><h3 id="basics" tabindex="-1">Basics <a class="header-anchor" href="#basics" aria-label="Permalink to &quot;Basics&quot;">​</a></h3><p>The Duktape command line tool writes out an allocation log when requested:</p><pre><code># Log written to /tmp/duk-alloc-log.txt
$ make clean duk
$ ./duk --alloc-logging tests/ecmascript/test-dev-mandel2-func.js
</code></pre><p>The &quot;duk-low&quot; command line tool is a variant with a simple pool allocator, and a host of low memory optimizations. It represents a low memory target quite well and it can also be requested to write out an allocation log:</p><pre><code># Log written to /tmp/lowmem-alloc-log.txt
$ make clean duk-low
$ ./duk-low --lowmem-log tests/ecmascript/test-dev-mandel2-func.js
</code></pre><p>Allocation logs are represented in examples/alloc-logging format:</p><pre><code>...
A 0xf7541c38 16
R 0xf754128c -1 0xf754125c 6
A 0xf7541c24 16
...
</code></pre><p>The pool simulator doesn&#39;t need to know the &quot;previous size&quot; for a realloc entry, so it can be written out as -1 (like duk-low does).</p><p>Pool configurations are expressed in JSON:</p><pre><code>{
  &quot;pools&quot;: [
    { &quot;size&quot;: 8, &quot;count&quot;: 10, &quot;borrow&quot;: true },
    { &quot;size&quot;: 12, &quot;count&quot;: 10, &quot;borrow&quot;: true },
    { &quot;size&quot;: 16, &quot;count&quot;: 200, &quot;borrow&quot;: true },
    ...
  ]
}
</code></pre><p>The &quot;size&quot; (entry size, byte size) of a pool is the byte-size of individual chunks in that pool. The &quot;count&quot; (entry count) is the number of chunks preallocated for that pool. Above, the second pool has entry size of 12 bytes and a count of 10, for a total of 120 bytes.</p><p>The pool simulator has simplistic behavior:</p><ul><li>Allocations are taken from smallest matching pool. Borrowing from a larger pool is allowed if the smallest matching pool is out of chunks.</li><li>Reallocation tries to shrink the allocation to a previous pool size if possible.</li></ul><p>&quot;High-water-mark&quot; (hwm) over an entire allocation log means simulating the allocation log against a certain pool configuration, and recording the highest number of used entries for each pool. There are two variants for this measurement:</p><ul><li>Without borrowing: ignore the &quot;count&quot; for each pool in the configuration and autoextend the pool as needed. This provides a high-water-mark without a need to borrow from larger pools.</li><li>With borrowing: respect the &quot;count&quot; in the pool configuration and borrow as needed.</li></ul><h3 id="tight-pool-counts-using-high-water-mark-hwm" tabindex="-1">Tight pool counts using high water mark (hwm) <a class="header-anchor" href="#tight-pool-counts-using-high-water-mark-hwm" aria-label="Permalink to &quot;Tight pool counts using high water mark (hwm)&quot;">​</a></h3><p>To find out the high water mark for each pool size without borrowing:</p><pre><code>$ rm -rf /tmp/out; mkdir /tmp/out
$ python examples/alloc-logging/pool_simulator.py \\
    --out-dir /tmp/out \\
    --alloc-log /tmp/duk-alloc-log.txt \\
    --pool-config examples/alloc-logging/pool_config_1.json \\
    --out-pool-config /tmp/tight_noborrow.json \\
    tight_counts_noborrow
</code></pre><p>The hwm records the maximum count for each pool size:</p><pre><code>^ pool entry count
|
|   ##
|  #####
| ######
| ######
| ########
+---------&gt; pool entry size
</code></pre><p>As described above, this command ignores the pool counts in the pool config and autoextends each pool to find its hwm. The resulting pool configuration with updated counts is written out.</p><h3 id="tight-pool-counts-taking-borrowing-into-account" tabindex="-1">Tight pool counts taking borrowing into account <a class="header-anchor" href="#tight-pool-counts-taking-borrowing-into-account" aria-label="Permalink to &quot;Tight pool counts taking borrowing into account&quot;">​</a></h3><p>The high water marks for each pool entry size don&#39;t necessarily happen at the same time. Let&#39;s use the example above:</p><pre><code>^ pool entry count
|
|   ##
|  #####
| ######
| ######
| ########
+---------&gt; pool entry size
</code></pre><p>As an example, when the hwm for the third pool size (highlighted below) happens, the allocation state might be:</p><pre><code>^ pool entry count
|
|   #
|  :#
| ::#::
| ::#:::
| ::#:::::
+---------&gt; pool entry size
</code></pre><p>This means that we can often reduce the hwm-based pool counts and still allow the application to run; the application will be able to borrow allocations from larger pool entry sizes.</p><p>As an extreme example, if Duktape were to allocate and free one entry from each pool entry size (but so that only one allocation would be active at a time), the hwm counts would look like:</p><pre><code>^ pool entry count
|
|
|
|
|
| ########
+---------&gt; pool entry size
</code></pre><p>However, the allocations can all be satisfied by having just one pool entry of the largest allocated size: all other allocation requests will just borrow from that (assuming borrowing is allowed):</p><pre><code>^ pool entry count
|
|
|
|
|
|        #
+---------&gt; pool entry size
</code></pre><p>The pool simulator optimizes for tight pool counts with borrowing effects taken into account using a pretty simple brute force algorithm:</p><ul><li>Get the basic hwm profile with no borrowing.</li><li>Start from the largest pool entry size and loop downwards: <ul><li>Reduce pool entry count for that pool entry size in question and rerun the allocation log.</li><li>If allocation requests can be still satisfied through borrowing, continue to reduce the allocation.</li><li>When the pool entry count can no longer be reduced, move on to the next pool size.</li></ul></li></ul><p>The basic observation in the algorithm is as follows:</p><ul><li>The pool entry counts above the current one are optimal: they can&#39;t be reduced further.</li><li>The pool entry counts below the current one never borrow from any of the higher pool counts (yet) because they were optimized for their hwm.</li><li>We reduce the current pool entry count, hoping that some of the allocations needed for its hwm can be borrowed from the larger pool entry sizes. This is possible if the hwm of the current pool entry size doesn&#39;t coincide with the hwm of the larger pool entry sizes.</li></ul><p>This algorithm leads to reasonable pool entry counts, but:</p><ul><li>The counts may not be an optimal balance for other applications.</li><li>The pool entry sizes are assumed to be given and are not optimized for automatically.</li></ul><p>Use the following command to run the optimization:</p><pre><code>$ rm -rf /tmp/out; mkdir /tmp/out
$ python examples/alloc-logging/pool_simulator.py \\
    --out-dir /tmp/out \\
    --alloc-log /tmp/duk-alloc-log.txt \\
    --pool-config examples/alloc-logging/pool_config_1.json \\
    --out-pool-config /tmp/tight_borrow.json \\
    tight_counts_borrow
</code></pre><p>This may take a lot of time, so be patient.</p><p>As a concrete example, for test-dev-mandel2-func.js on x86 with low memory optimizations, the tight pool configuration based on hwm is:</p><pre><code>total 31564:
8=91 12=25 16=373 20=56 24=2 28=58 32=1 40=32 48=4 52=27 56=1 60=5 64=0
128=20 256=9 512=8 1024=4 1360=1 2048=2 4096=0 8192=0 16384=0 32768=0
</code></pre><p>and after borrow optimization:</p><pre><code>total 28532:
8=91 12=20 16=370 20=53 24=2 28=58 32=0 40=10 48=3 52=26 56=1 60=4 64=0
128=16 256=8 512=8 1024=3 1360=1 2048=2 4096=0 8192=0 16384=0 32768=0
</code></pre><p>The more dynamic an application&#39;s memory usage is, the more potential there is for borrowing.</p><h3 id="optimizing-for-multiple-application-profiles" tabindex="-1">Optimizing for multiple application profiles <a class="header-anchor" href="#optimizing-for-multiple-application-profiles" aria-label="Permalink to &quot;Optimizing for multiple application profiles&quot;">​</a></h3><p>Run hello world with alloc logging for Duktape baseline:</p><pre><code># Using &quot;duk&quot;, writes log to /tmp/duk-alloc-log.txt
$ ./duk --alloc-logging tests/ecmascript/test-dev-hello-world.js

# Using &quot;duk-low&quot;, writes log to /tmp/lowmem-alloc-log.txt
$ ./duk-low --lowmem-log tests/ecmascript/test-dev-hello-world.js
</code></pre><p>Extract a &quot;tight&quot; pool configuration for the hello world baseline, pool entry sizes (but not counts) need to be known in advance:</p><pre><code>$ rm -rf /tmp/out; mkdir /tmp/out
$ python examples/alloc-logging/pool_simulator.py \\
    --out-dir /tmp/out \\
    --alloc-log /tmp/duk-alloc-log.txt \\
    --pool-config examples/alloc-logging/pool_config1.json \\
    --out-pool-config /tmp/config_tight_duktape.json \\
    tight_counts_borrow
</code></pre><p>Run multiple test applications and extract tight pool configurations for each (includes Duktape baseline but that is subtracted later) using the same method:</p><pre><code>$ ./duk --alloc-logging tests/ecmascript/test-dev-mandel2-func.js
$ rm -rf /tmp/out; mkdir /tmp/out
$ python examples/alloc-logging/pool_simulator.py \\
    --out-dir /tmp/out \\
    --alloc-log /tmp/duk-alloc-log.txt \\
    --pool-config examples/alloc-logging/pool_config1.json \\
    --out-pool-config /tmp/config_tight_app1.json \\
    tight_counts_borrow

$ ./duk --alloc-logging tests/ecmascript/test-bi-array-proto-push.js
$ rm -rf /tmp/out; mkdir /tmp/out
$ python examples/alloc-logging/pool_simulator.py \\
    --out-dir /tmp/out \\
    --alloc-log /tmp/duk-alloc-log.txt \\
    --pool-config examples/alloc-logging/pool_config1.json \\
    --out-pool-config /tmp/config_tight_app2.json \\
    tight_counts_borrow

# ...
</code></pre><p>Select a target memory amount (here 200kB) and optimize pool entry counts for that amount:</p><pre><code>$ python examples/alloc-logging/pool_simulator.py \\
    --out-pool-config /tmp/config_200kb.json \\
    --out-ajsheap-config /tmp/ajsheap_200kb.c \\
    pool_counts_for_memory \\
    204800 \\
    /tmp/config_tight_duktape.json \\
    /tmp/config_tight_app1.json \\
    /tmp/config_tight_app2.json \\
    ... \\
    /tmp/config_tight_appN.json

# /tmp/config_200kb.json is the pool config in JSON

# /tmp/ajsheap_200kb.c is the pool config as an ajs_heap.c initializer
</code></pre><p>The optimization algorithm is based on the following basic idea:</p><ul><li>Pool entry byte sizes are kept fixed throughout the process.</li><li>Application pool counts are normalized by subtracting Duktape baseline pool counts, yielding application memory usage on top of Duktape. These pool counts can be scaled meaningfully to estimate memory demand if the &quot;application size&quot; (function count, statement count, etc) were to grow or shrink.</li><li>The resulting pool count profiles are normalized to a fixed total memory usage (any value will do, 1MB is used now). The resulting pool counts are fractional.</li><li>A pool count profile representing all the applications is computed as follows. For each pool entry size, take the maximum of the normalized, scaled pool counts over the application profiles. This profile represents the the memory usage of a mix of applications.</li><li>Allocate pool counts for Duktape baseline. This allocation is independent of application code and doesn&#39;t grow in relation to application memory usage.</li><li>Scale the representative pool count profile to fit the remaining memory, using fractional counts.</li><li>Round pool counts into integers, ensuring the total memory usage is as close to the target (without exceeding it).</li></ul><h2 id="summary-of-potential-measures" tabindex="-1">Summary of potential measures <a class="header-anchor" href="#summary-of-potential-measures" aria-label="Permalink to &quot;Summary of potential measures&quot;">​</a></h2><h3 id="heap-headers" tabindex="-1">Heap headers <a class="header-anchor" href="#heap-headers" aria-label="Permalink to &quot;Heap headers&quot;">​</a></h3><ul><li>Compressed 16-bit heap pointers</li><li>16-bit field for refcount</li><li>Move one struct specific field (e.g. 16-bit string length) into the unused bits of the <code>duk_heaphdr</code> 32-bit flags field</li></ul><h3 id="objects" tabindex="-1">Objects <a class="header-anchor" href="#objects" aria-label="Permalink to &quot;Objects&quot;">​</a></h3><ul><li>Tweak growth factors to keep objects always or nearly always compact</li><li>16-bit field for property count, array size, etc.</li><li>Drop hash part entirely: it&#39;s rarely needed in low memory environments and hash part size won&#39;t need to be tracked</li><li>Compressed pointers</li></ul><h3 id="strings" tabindex="-1">Strings <a class="header-anchor" href="#strings" aria-label="Permalink to &quot;Strings&quot;">​</a></h3><ul><li>Use an indirect string type which stores string data behind a pointer (same as dynamic buffer); allow user code to indicate which C strings are immutable and can be used in this way</li><li>Allow user code to move a string to e.g. memory-mapped flash when it is interned or when the compiler interns its constants (this is referred to as &quot;static strings&quot; or &quot;external strings&quot;)</li><li>Memory map built-in strings (about 2kB bit packed) directly from flash</li><li>16-bit fields for string char and byte length</li><li>16-bit string hash</li><li>Compressed pointers</li></ul><h3 id="duktape-c-function-footprint" tabindex="-1">Duktape/C function footprint <a class="header-anchor" href="#duktape-c-function-footprint" aria-label="Permalink to &quot;Duktape/C function footprint&quot;">​</a></h3><ul><li>Lightweight functions, converting built-ins into lightweight functions</li><li>Lightweight functions for user Duktape/C binding functions</li><li>Magic value to share native code cheaply for multiple function objects</li><li>Compressed pointers</li></ul><h3 id="ecmascript-function-footprint" tabindex="-1">ECMAScript function footprint <a class="header-anchor" href="#ecmascript-function-footprint" aria-label="Permalink to &quot;ECMAScript function footprint&quot;">​</a></h3><ul><li>Motivation <ul><li>Small lexically nested callbacks are often used in ECMAScript code, so it&#39;s important to keep their size small</li></ul></li><li>Reduce property count: <ul><li>_pc2line: can be dropped, lose line numbers in tracebacks</li><li>_formals: can be dropped for most functions (affects debugging)</li><li>_varmap: can be dropped for most functions (affects debugging)</li></ul></li><li>Reduce compile-time maximum alloc size for bytecode: currently each instruction takes 8 bytes, 4 bytes for the instruction itself and 4 bytes for line number. Change this into two allocations so that the maximum allocation size is not double that of final bytecode, as that is awkward for pool allocators.</li><li>Improve property format, e.g. <code>_Formals</code> is now a regular array which is quite wasteful; it could be converted to a <code>\\xFF</code> separated string for instance.</li><li>Spawn <code>.prototype</code> on demand to eliminate one unnecessary object per function</li><li>Use virtual properties when possible, e.g. if <code>nargs</code> equals desired <code>length</code>, use virtual property for it (either non-writable or create concrete property when written)</li><li>Write bytecode and pc2line to flash during compilation</li><li>Compressed pointers</li></ul><h3 id="contiguous-allocations" tabindex="-1">Contiguous allocations <a class="header-anchor" href="#contiguous-allocations" aria-label="Permalink to &quot;Contiguous allocations&quot;">​</a></h3><p>Unbounded contiguous allocations are a problem for pool allocators. There are at least the following sources for these:</p><ul><li>Large user strings and buffers. Not much can be done about these without a full rework of the Duktape C programming model (which assumes string and buffer data is available as plain <code>const char *</code>).</li><li>Bytecode/const buffer for long ECMAScript functions: <ul><li>Bytecode and constants can be placed in separate buffers.</li><li>Bytecode could be &quot;segmented&quot; so that bytecode would be stored in chunks (e.g. 64 opcodes = 256 bytes). An explicit JUMP to jump from page to page could make the executor impact minimal.</li><li>During compilation Duktape uses a single buffer to track bytecode instructions and their line numbers. This takes 8 bytes per instruction while the final bytecode takes 4 bytes per instruction. This is easy to fix by using two separate buffers.</li></ul></li><li>Value stacks of Duktape threads. Start from 1kB and grow without (practical) bound depending on call nesting.</li><li>Catch and call stacks of Duktape threads. Also contiguous but since these are much smaller, they&#39;re unlikely to be a problem before the value stack becomes one.</li></ul><h2 id="notes-on-function-memory-footprint" tabindex="-1">Notes on function memory footprint <a class="header-anchor" href="#notes-on-function-memory-footprint" aria-label="Permalink to &quot;Notes on function memory footprint&quot;">​</a></h2><h3 id="normal-function-representation" tabindex="-1">Normal function representation <a class="header-anchor" href="#normal-function-representation" aria-label="Permalink to &quot;Normal function representation&quot;">​</a></h3><p>In Duktape 1.0.0 functions are represented as:</p><ul><li>A <code>duk_hcompfuncn</code> (a superset of <code>duk_hobject</code>): represents an ECMAScript function which may have a set of properties, and points to the function&#39;s data area (bytecode, constants, inner function refs).</li><li>A <code>duk_hnatfunc</code> (a superset of <code>duk_hobject</code>): represents a Duktape/C function which may also have a set of properties. A pointer to the C function is inside the <code>duk_hnatfunc</code> structure.</li></ul><p>In Duktape 1.1.0 a lightfunc type is available:</p><ul><li>A lightfunc is an 8-byte <code>duk_tval</code> with no heap allocations, and provides a cheap way to represent many Duktape/C functions.</li></ul><p>RAM footprints for each type are discussed below.</p><h3 id="ecmascript-functions" tabindex="-1">ECMAScript functions <a class="header-anchor" href="#ecmascript-functions" aria-label="Permalink to &quot;ECMAScript functions&quot;">​</a></h3><p>An ordinary ECMAScript function takes around 300-500 bytes of RAM. There are three objects involved:</p><ul><li>a function template</li><li>a function instance (multiple instances can be created from one template)</li><li>automatic prototype object allocated for the function instance</li></ul><p>The function template is used to instantiate a function. The resulting function is not dependent on the template after creation, so that the template can be garbage collected. However, the template often remains reachable in callback style programming, through the enclosing function&#39;s inner function templates table.</p><p>The function instance contains a <code>.prototype</code> property while the prototype contains a <code>.constructor</code> property, so that both functions require a property table. This is the case even for the majority of user functions which will never be used as constructors; built-in functions are oddly exempt from having an automatic prototype.</p><h3 id="duktape-c-functions" tabindex="-1">Duktape/C functions <a class="header-anchor" href="#duktape-c-functions" aria-label="Permalink to &quot;Duktape/C functions&quot;">​</a></h3><p>A Duktape/C function takes about 70-80 bytes of RAM. Unlike ECMAScript functions, Duktape/C function are already stripped of unnecessary properties and don&#39;t have an automatic prototype object.</p><p>Even so, there are close to 200 built-in functions, so the footprint of the <code>duk_hnatfunc</code> objects is around 14-16kB, not taking into account allocator overhead.</p><h3 id="duktape-c-lightfuncs" tabindex="-1">Duktape/C lightfuncs <a class="header-anchor" href="#duktape-c-lightfuncs" aria-label="Permalink to &quot;Duktape/C lightfuncs&quot;">​</a></h3><p>Lightfuncs require only a <code>duk_tval</code>, 8 bytes. There are no additional heap allocations.</p>`,157)]))}const h=o(a,[["render",l]]);export{m as __pageData,h as default};
