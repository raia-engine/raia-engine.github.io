import{_ as o,c as t,a2 as a,o as n}from"./chunks/framework.DPuwY6B9.js";const h=JSON.parse('{"title":"Duktape 2.0 release notes","description":"","frontmatter":{},"headers":[],"relativePath":"resource/duktape/doc/release-notes-v2-0.md","filePath":"resource/duktape/doc/release-notes-v2-0.md","lastUpdated":1732350347000}'),i={name:"resource/duktape/doc/release-notes-v2-0.md"};function r(d,e,s,l,c,u){return n(),t("div",null,e[0]||(e[0]=[a(`<h1 id="duktape-2-0-release-notes" tabindex="-1">Duktape 2.0 release notes <a class="header-anchor" href="#duktape-2-0-release-notes" aria-label="Permalink to &quot;Duktape 2.0 release notes&quot;">​</a></h1><h2 id="release-overview" tabindex="-1">Release overview <a class="header-anchor" href="#release-overview" aria-label="Permalink to &quot;Release overview&quot;">​</a></h2><p>Main changes in this release (see RELEASES.rst for full details):</p><ul><li>New tools/configure.py frontend tool replaces genconfig.py for configuring and preparing Duktape sources for build.</li><li>Buffer handling has been simplified: Duktape.Buffer has been removed and is replaced by Uint8Array, plain buffers now behave like Uint8Array objects. Node.js Buffer behavior aligned with more recent Node.js Buffer API.</li><li>Implement more ES2015 and ES2016 functionality, and align some ES5.1 semantics with ES2015/ES2016. Implement WHATWG Encoding API with TextEncoder() and TextDecoder() bindings.</li><li>Some incompatible API changes, and several API additions. API and config changes to avoid I/O dependencies (such as printf() and fopen()) in core Duktape code to simplify porting.</li><li>More configuration flexibility in dropping Duktape specific functionality from build, e.g. coroutines and finalization.</li><li>Disabled ECMAScript bindings are no longer present (instead of being present but throwing a TypeError).</li><li>Built-in functionality moved to optional extras: print/alert bindings, logging, and module loader. New optional extras include a Node.js-like module loader and a &#39;console&#39; binding.</li><li>Bug fixes, performance and footprint improvements.</li></ul><p>The release has API incompatible changes, see upgrading notes below.</p><h2 id="upgrading-from-duktape-1-x" tabindex="-1">Upgrading from Duktape 1.x <a class="header-anchor" href="#upgrading-from-duktape-1-x" aria-label="Permalink to &quot;Upgrading from Duktape 1.x&quot;">​</a></h2><p>There are API incompatible changes in this release. Whenever possible the incompatible changes cause a compilation error (or warning) so that fixing call sites should be straightforward. Below are instructions on how to migrate from 1.x to 2.0.0. There are also bug fixes and other minor behavioral changes which may affect some applications, see <code>RELEASES.rst</code> for details.</p><p>There are backwards compatible providers for some removed/modified API calls in <code>extras/duk-v1-compat</code>.</p><h3 id="supporting-duktape-1-x-and-duktape-2-x-simultaneously" tabindex="-1">Supporting Duktape 1.x and Duktape 2.x simultaneously <a class="header-anchor" href="#supporting-duktape-1-x-and-duktape-2-x-simultaneously" aria-label="Permalink to &quot;Supporting Duktape 1.x and Duktape 2.x simultaneously&quot;">​</a></h3><p>For C code you can use the <code>DUK_VERSION</code> define to support both Duktape 1.x and 2.x in the same application. For example:</p><pre><code>#if (DUK_VERSION &gt;= 20000)
rc = duk_safe_call(ctx, my_safe_call, NULL, 1 /*nargs*/, 1 /*nrets*/);
#else
rc = duk_safe_call(ctx, my_safe_call, 1 /*nargs*/, 1 /*nrets*/);
#endif
</code></pre><p>If you&#39;re developing against Duktape master before 2.0 release, <code>DUK_VERSION</code> is set to 19999 so that you can use:</p><pre><code>#if (DUK_VERSION &gt;= 19999)
rc = duk_safe_call(ctx, my_safe_call, NULL, 1 /*nargs*/, 1 /*nrets*/);
#else
rc = duk_safe_call(ctx, my_safe_call, 1 /*nargs*/, 1 /*nrets*/);
#endif
</code></pre><p>Similarly for ECMAScript code you can:</p><pre><code>var plainBuffer;
if (Duktape.version &gt;= 19999) {
    plainBuffer = Uint8Array.plainOf(bufferObject);
} else {
    plainBuffer = Duktape.Buffer(bufferObject);
}
</code></pre><p>Or you can detect features specifically:</p><pre><code>var plainBuffer = (typeof Uint8Array.plainOf === &#39;function&#39; ?
                   Uint8Array.plainOf : Duktape.Buffer)(bufferObject);
</code></pre><h3 id="duk-opt-xxx-feature-option-support-removed" tabindex="-1">DUK_OPT_xxx feature option support removed <a class="header-anchor" href="#duk-opt-xxx-feature-option-support-removed" aria-label="Permalink to &quot;DUK_OPT_xxx feature option support removed&quot;">​</a></h3><p>Duktape 2.0 no longer supports <code>DUK_OPT_xxx</code> options given via the compiler command line. Instead, all options are encoded in <code>duk_config.h</code>.</p><p>To use custom Duktape options, use the <code>tools/configure.py</code> tool to create a customized <code>duk_config.h</code> and prepared Duktape sources matching the configuration. For example to enable assertions and fastint support:</p><pre><code>$ python2 tools/configure.py \\
      --output-directory /tmp/output \\
      --source-directory src-input \\
      --config-metadata config \\
      -DDUK_USE_FASTINT \\
      -DDUK_USE_ASSERTIONS

# Prepared duk_config.h header and Duktape sources (duktape.h and
# duktape.c) are in /tmp/output.  Compile normally with your application.

$ gcc -std=c99 -Wall -o/tmp/test -I/tmp/output /tmp/output/duktape.c \\
      my_application.c -lm
</code></pre><p>See <a href="http://wiki.duktape.org/Configuring.html" target="_blank" rel="noreferrer">http://wiki.duktape.org/Configuring.html</a> for details and examples.</p><p>To upgrade:</p><ul><li>If you&#39;re using the Duktape default configuration and no <code>DUK_OPT_xxx</code> compiler options, no actions are needed.</li><li>Otherwise, remove <code>DUK_OPT_xxx</code> options from the compilation command and add a <code>tools/configure.py</code> pre-step to your build. Add the equivalent <code>DUK_USE_xxx</code> options to <code>tools/configure.py</code> argument list; for example <code>-DDUK_USE_FASTINT</code>.</li><li>If you&#39;re using a <code>duk_custom.h</code> header there are three simple approaches: <ul><li>To embed your custom header into <code>duk_config.h</code> statically, use <code>--fixup-file duk_custom.h</code> in <code>tools/configure.py</code> options.</li><li>To include your custom header at compilation time, similarly to <code>DUK_OPT_HAVE_CUSTOM_H</code>, use <code>--fixup-line &#39;#include &quot;duk_custom.h&quot;&#39;</code> in <code>tools/configure.py</code> options.</li><li>Finally, you can in some cases remove your custom header and use equivalent config options for <code>tools/configure.py</code>.</li></ul></li></ul><h3 id="config-option-changes" tabindex="-1">Config option changes <a class="header-anchor" href="#config-option-changes" aria-label="Permalink to &quot;Config option changes&quot;">​</a></h3><p>There are several new config options and some existing config options have been removed.</p><p>To upgrade:</p><ul><li>Review any <code>DUK_OPT_xxx</code> or <code>DUK_USE_xxx</code> options in use against <code>config/config-options/*.yaml</code>.</li></ul><h3 id="built-ins-disabled-in-configuration-are-now-absent" tabindex="-1">Built-ins disabled in configuration are now absent <a class="header-anchor" href="#built-ins-disabled-in-configuration-are-now-absent" aria-label="Permalink to &quot;Built-ins disabled in configuration are now absent&quot;">​</a></h3><p>If a built-in is disabled when running <code>configure.py</code>, it won&#39;t be present in the ECMAScript environment. For example, with <code>-UDUK_USE_ES6_PROXY</code>:</p><pre><code>duk&gt; new Proxy()
ReferenceError: identifier &#39;Proxy&#39; undefined
    at [anon] (duk_js_var.c:1262) internal
    at global (input:1) preventsyield
duk&gt; typeof Proxy
= &quot;undefined&quot;
</code></pre><p>In Duktape 1.x the binding was present but would just throw an Error when invoked:</p><pre><code>duk&gt; new Proxy()
Error: unknown error (rc -1)
    at Proxy () native strict construct preventsyield
    at global (input:1) preventsyield
duk&gt; typeof Proxy
= &quot;function&quot;
</code></pre><p>The revised behavior saves footprint and allows scripts to detect supported built-ins reliably using e.g.:</p><pre><code>if (typeof Proxy === &#39;function&#39;) {
    // supported
}
</code></pre><p>To upgrade:</p><ul><li>In most cases no action is needed. If your code relies on the builtins being present but throwing an error (which seems unlikely), such call sites need to be fixed.</li></ul><h3 id="tooling-changes" tabindex="-1">Tooling changes <a class="header-anchor" href="#tooling-changes" aria-label="Permalink to &quot;Tooling changes&quot;">​</a></h3><p>There are some tooling changes in this release:</p><ul><li>The distributable now includes raw sources in <code>src-input/</code> and some tooling in <code>tools/</code>. This allows Duktape sources to be modified and re-amalgamated directly from the distributable. The distributable still includes sources prepared using default configuration (<code>src/</code>, <code>src-noline/</code>, and <code>src-separate</code>) and some configuration examples.</li><li>The tooling includes a new <code>tools/configure.py</code> tool which creates a <code>duk_config.h</code> and matching prepared sources simultaneously. This allows use of ROM built-ins from the distributable. Previously ROM built-ins required a manual <code>dist.py --rom-support ...</code> command.</li><li>The <code>make_dist.py</code> utility in Duktape main repo has been renamed to <code>dist.py</code> and no longer supports <code>--rom-support</code>, <code>--rom-auto-lightfunc</code>, and <code>--user-builtin-metadata</code> options. Use the <code>tools/configure.py</code> tool instead, which supports these options. However, <code>--user-builtin-metadata</code> has been renamed <code>--builtin-file</code>.</li><li>The <code>config/genconfig.py</code> has been relocated to <code>tools/genconfig.py</code> in the distributable. It can still be used as a standalone tool, but using configure.py is recommended instead.</li></ul><p>To upgrade:</p><ul><li>If you&#39;re just using the default sources and <code>duk_config.h</code> in the distributable, no changes are needed.</li><li>If you&#39;re using <code>genconfig.py</code> considering using <code>tools/configure.py</code> instead. If you keep on using <code>genconfig.py</code>, update path to <code>tools/genconfig.py</code>.</li><li>If you&#39;re using ROM built-ins via <code>make_dist.py</code>, change your build to use <code>tools/configure.py</code> instead, and change <code>--user-builtins-metadata</code> option argument(s) to <code>--builtin-file</code>.</li></ul><h3 id="dist-package-file-changes" tabindex="-1">Dist package file changes <a class="header-anchor" href="#dist-package-file-changes" aria-label="Permalink to &quot;Dist package file changes&quot;">​</a></h3><ul><li>Configuration metadata is now in unpacked form in <code>dist/config</code> to match the Duktape master repo and to make config files more convenient to patch. The <code>dist/tools/genconfig.py</code> tool no longer accepts a tar.gz metadata argument.</li><li>The pre-built <code>duk_config.h</code> examples have been removed as somewhat useless. Use <code>dist/tools/configure.py</code> (or <code>dist/tools/genconfig.py)</code> to generate <code>duk_config.h</code> files.</li><li><code>dist/duk_build_meta.json</code> has been renamed to <code>dist/duk_dist_meta.json</code> for clarity. It no longer contains string data scanned from source files. This metadata is now in source directories, e.g. <code>dist/src/duk_source_meta.json</code> as the string set potentially depends on options used to prepare sources.</li><li>Source metadata, e.g. <code>dist/src/metadata.json</code>, has been renamed to <code>dist/src/duk_source_meta.json</code> for clarity. The metadata contains Duktape version information, strings scanned from source files, and for combined (amalgamated) sources the line number metadata.</li></ul><h3 id="buffer-behavior-changes" tabindex="-1">Buffer behavior changes <a class="header-anchor" href="#buffer-behavior-changes" aria-label="Permalink to &quot;Buffer behavior changes&quot;">​</a></h3><p>There are a lot of buffer behavior changes in the 2.0 release; see detailed changes below and in RELEASES.rst. Here&#39;s a summary of changes:</p><ul><li><code>Duktape.Buffer</code> has been removed. Plain buffers now behave like <code>Uint8Array</code> instances to the extent possible. They don&#39;t have a property table, however, which causes some limitations. Plain buffers ToObject() coerce to an actual Uint8Array object with the same backing buffer. There are many small changes to how plain buffers are treated by standard built-ins as a result. For example, string coercion (<code>String(plainBuffer)</code>) now mimics Uint8Array and usually results in the string <code>[object Uint8Array]</code>.</li><li>Plain buffers have an inherited <code>.buffer</code> getter property which returns an ArrayBuffer object backing to the same underlying plain buffer. Because there is no property table for plain buffers, each <code>.buffer</code> access creates a new ArrayBuffer instance.</li><li>When <code>duk_push_buffer_object()</code> creates an automatic ArrayBuffer for a view (such as Uint8Array), the ArrayBuffer&#39;s .byteOffset will be set to 0 and its .byteLength will be set to view.byteOffset + view.byteLength. This ensures that accessing the ArrayBuffer at view.byteOffset returns the same value as when accessing view at index 0, which is the usual relationship between a view and its backing ArrayBuffer. Up to Duktape 1.6.x the ArrayBuffer&#39;s .byteOffset and .byteLength would be the same as the view&#39;s.</li><li>Non-standard properties, such as virtual indices and <code>.length</code> have been removed from ArrayBuffer and DataView. The <code>.byteOffset</code>, <code>.byteLength</code>, <code>.BYTES_PER_ELEMENT</code>, and <code>.buffer</code> properties of view objects are now inherited getters to match ES2015. The <code>.length</code> property remains a virtual own property, however (it is a getter in ES2015).</li><li>Default ECMAScript built-ins no longer provide the ability to do a 1:1 buffer-to-string coercion where the buffer bytes are used directly as the internal string bytes. Instead, an encoding (usually UTF-8) is always involved, and U+FFFD replacement characters are used when invalid inputs are encountered. See <a href="https://github.com/svaarala/duktape/issues/1005" target="_blank" rel="noreferrer">https://github.com/svaarala/duktape/issues/1005</a>. C code can still do 1:1 conversions using <code>duk_buffer_to_string()</code> or by pushing a raw string directly, and can expose such a binding to ECMAScript code.</li><li>Node.js Buffer binding has been aligned more with Node.js v6.9.1 (from Node.js v0.12.1) and some (but not all) behavior differences to actual Node.js have been fixed.</li><li>Disabling <code>DUK_USE_BUFFEROBJECT_SUPPORT</code> allows use of plain buffers in the C API, and allows manipulation of plain buffers in ECMAScript code via their virtual properties (index properties, <code>.length</code>, etc). Plain buffers still inherit from <code>Uint8Array.prototype</code>, but won&#39;t Object coerce. All ArrayBuffer, typed array, and Node.js Buffer methods will be missing, including <code>Uint8Array.allocPlain()</code>. Duktape custom built-ins operating on plain buffers (like Duktape.dec() with hex or base-64 encoding) continue to work. (This behavior is not guaranteed and may change even in minor versions.)</li></ul><p>To upgrade:</p><ul><li>If you&#39;re using buffers in general, review <a href="http://wiki.duktape.org/HowtoBuffers.html" target="_blank" rel="noreferrer">http://wiki.duktape.org/HowtoBuffers.html</a> which has been updated for Duktape 2.0.</li><li>If you&#39;re using standard ArrayBuffers and typed arrays, no changes should normally be necessary, however see technical changes in RELEASES.rst.</li><li>If you&#39;re using the Node.js Buffer binding, review the following: <ul><li>Node.js Buffer <code>.concat()</code> always returns a buffer copy, even for a one-element input array which had special handling in Node.js v0.12.1.</li><li>Node.js Buffer.prototype <code>.toString()</code> now decodes the input buffer using UTF-8, emitting replacement characters for invalid UTF-8 sequences.</li><li>Review Buffer code for Node.js Buffer changes between Node.js versions v0.12.1 and v6.9.1 in general.</li></ul></li><li>If you&#39;re using plain buffers, review their usage especially in ECMAScript code, in particular: <ul><li>Because plain buffers now mimic Uint8Array (a view), they are treated as initializer values when used as typed array constructor arguments. For example, <code>new Uint32Array(plainBuffer)</code> will create a new Uint32Array rather than a view into the plain buffer.</li><li>To create a view into the plain buffer, use the same approach as with a Uint8Array, e.g. <code>new Uint32Array(plainBuffer.buffer)</code>.</li></ul></li><li>Regardless of buffer type(s) in use: <ul><li>One important change is that <code>String(plainBuffer)</code> and <code>duk_to_string()</code> for a buffer does not work as before, use new <code>duk_buffer_to_string()</code> C API call instead. There&#39;s no equivalent function for the default ECMAScript built-ins.</li><li>Another important change is that plain buffers, like Uint8Array objects, boolean coerce to <code>true</code> regardless of buffer size (zero or larger) and contents.</li></ul></li><li>If you&#39;re using <code>Duktape.Buffer</code>, the following new built-ins replace its functionality (and more): <ul><li><code>Uint8Array.allocPlain()</code>: to allocate a new (fixed) plain buffer</li><li><code>Uint8Array.plainOf()</code>: to get the underlying plain buffer of any buffer object (without making a copy)</li><li>However, these bindings are intentionally missing if buffer object support has been disabled in Duktape configuration.</li></ul></li></ul><p>Some detailed changes, not exhaustive; see <code>RELEASES.rst</code> and <code>tests/ecmascript/test-bi-plain-buffer-*.js</code> for even more detail:</p><ul><li><code>typeof plainBuffer</code> is now <code>object</code> instead of <code>buffer</code>.</li><li><code>plainBuffer instanceof Uint8Array</code> is true.</li><li>Plain buffer Object.prototype.toString() now usually, assuming no overridden .toString(), yields <code>[object Uint8Array]</code> instead of <code>[object Buffer]</code>.</li><li>Plain buffer inherits from Uint8Array.prototype instead of Duktape.Buffer.prototype.</li><li>For a plain buffer <code>duk_to_string()</code> no longer creates a string with the same underlying bytes, but results in <code>[object Uint8Array]</code> instead (unless <code>.toString()</code> or <code>.valueOf()</code> has been overridden); in particular, using a plain buffer as an object property key is misleading as <code>obj[buf]</code> is (usually) equivalent to <code>obj[&#39;[object Uint8Array]&#39;]</code>. <code>duk_to_buffer()</code> for a string still results in a plain buffer with the same underlying bytes as before.</li><li>A new <code>duk_buffer_to_string()</code> API call converts any buffer value to a string with the same underlying bytes as in the buffer (like <code>duk_to_string()</code> did in Duktape 1.x). ECMAScript built-ins no longer have this ability directly.</li><li><code>duk_to_boolean()</code> for a plain buffer: always true, even if the buffer has zero length.</li><li><code>duk_to_primitive()</code> for plain buffer: usually coerces to the string <code>[object Uint8Array]</code> because plain buffers are not considered a primitive value.</li><li><code>duk_is_primitive()</code> for a plain buffer is now false to match how <code>duk_to_primitive()</code> deals with plain buffers (i.e. coerces them rather than returning them as is).</li><li>When a plain buffer is used as the &quot;this&quot; binding of a function call, it is ToObject() coerced to an actual Uint8Array if the call target is non-strict. This mimics what happens to e.g. plain strings. Lightfuncs have also been revised to behave the same way (in Duktape 1.x they would not be ToObject() coerced in this situation).</li><li><code>new ArrayBuffer(plainBuffer)</code> no longer creates a new ArrayBuffer with the same underlying plain buffer; instead, the plain buffer gets coerced to zero and creates a zero-length ArrayBuffer. This matches how a Uint8Array argument is handled in <code>new ArrayBuffer()</code>.</li><li><code>new Buffer(plainBuffer)</code> no longer special cases plain buffer and gets treated like an Uint8Array: a fresh Buffer with matching <code>.length</code> is created and index elements are copied into the result buffer (in effect making an actual buffer copy).</li><li><code>ArrayBuffer.isView(nodejsBuffer)</code> is now true to reflect the fact that Node.js Buffers are Uint8Arrays in newer Node.js versions.</li><li><code>new Uint32Array(plainBuffer)</code> and other typed array constructors use the argument plain buffer as an initializer (like Uint8Array), which causes a copy to be created.</li><li><code>new DataView(plainBuffer)</code> is rejected and DataView() in general rejects any other argument than an actual ArrayBuffer.</li><li><code>typedarray.prototype.subarray()</code> accepts a plain buffer and the resulting slice is a Uint8Array because plain buffers cannot represent a view offset/length.</li><li>Node.js <code>Buffer.prototype.slice()</code> accepts a plain buffer and the result is a Node.js Buffer (which itself is a special Uint8Array instance).</li><li><code>plainBuffer.valueOf()</code> ordinarily backed by <code>Object.prototype.valueOf()</code> returns <code>Object(plainBuffer)</code>, i.e. converts plain buffer to an actual Uint8Array. This matches normal <code>Object.prototype.valueOf()</code> behavior, e.g. plain string is coerced into a String object.</li><li><code>JSON.stringify()</code> now recognizes plain buffers like Uint8Array instances; the result is typically <code>{&quot;0&quot;:XXX,&quot;1&quot;:XXX,....}</code> without a <code>.toJSON()</code> implementation, as the virtual index properties are enumerable for Uint8Arrays.</li><li><code>Object.freeze()</code> not allowed for plain buffers or buffer objects (Duktape 1.x allowed silently) because array index elements cannot be made non-writable. This is an internal limitation and failing with a TypeError signals this to the caller (and matches how e.g. V8 handles <code>Object.freeze(new Uint8Array(4))</code>).</li><li>Typed array <code>.subarray()</code> and Node.js Buffer <code>.slice()</code> result internal prototype is now set to the default prototype of the result type (e.g. initial value of <code>Uint8Array.prototype</code> if the input is an Uint8Array) rather than being copied from the argument.</li><li>Node.js <code>Buffer</code> and <code>Buffer.prototype</code> methods now accept plain buffers.</li><li>A plain buffer is accepted as a constructor &quot;replacement value&quot;.</li></ul><h3 id="pointer-behavior-changes" tabindex="-1">Pointer behavior changes <a class="header-anchor" href="#pointer-behavior-changes" aria-label="Permalink to &quot;Pointer behavior changes&quot;">​</a></h3><p>There are very minor changes to pointer value behavior:</p><ul><li><code>plainPointer instanceof Duktape.Pointer</code> now evaluates to <code>true</code> (<code>false</code> in Duktape 1.x).</li></ul><p>To upgrade:</p><ul><li>If you&#39;re using pointer values in ECMAScript code, check pointer handling.</li></ul><h3 id="lightfunc-behavior-changes" tabindex="-1">Lightfunc behavior changes <a class="header-anchor" href="#lightfunc-behavior-changes" aria-label="Permalink to &quot;Lightfunc behavior changes&quot;">​</a></h3><p>There are very minor changes to lightfunc value behavior:</p><ul><li><code>duk_is_primitive()</code> now returns false for lightfuncs; this is more in line with how lightfuncs behave in ECMAScript ToPrimitive() coercion and matches how plain buffers work in Duktape 2.x.</li><li><code>[[DefaultValue]]</code> coercion now considers lightfuncs non-primitive (previously considered primitive and thus accepted as <code>[[DefaultValue]]</code> result).</li><li>When a lightfunc is used as the &quot;this&quot; binding of a function call, it is ToObject() coerced to a full function when the call target is non-strict. Duktape 1.x would not coerce the lightfunc to an object in this situation; the change was made to match plain buffer behavior. Note that because lightfuncs themselves are considered strict functions, this only happens when the call target is not a lightfunc but the &quot;this&quot; binding is.</li><li>A lightfunc is accepted as a constructor &quot;replacement value&quot;.</li></ul><p>To upgrade:</p><ul><li>If you&#39;re using lightfuncs, review their handling.</li></ul><h3 id="print-and-alert-globals-removed" tabindex="-1">print() and alert() globals removed <a class="header-anchor" href="#print-and-alert-globals-removed" aria-label="Permalink to &quot;print() and alert() globals removed&quot;">​</a></h3><p>The <code>print()</code> and <code>alert()</code> globals were removed because they depended on stdout/stderr which is a portability issue on some platforms. Further, even if stdout/stderr is available, it&#39;s not always the appropriate place for debug printouts, so it&#39;s cleaner if the application provides its own debug/console logging functions.</p><p>To upgrade:</p><ul><li><p>If you don&#39;t use <code>print()</code> or <code>alert()</code> no action is needed; they simply won&#39;t be a part of the global object anymore.</p></li><li><p>If a simple <code>print()</code> and/or <code>alert()</code> suffices, you can use something like this:</p><pre><code>static duk_ret_t my_print(duk_context *ctx) {
    duk_push_string(ctx, &quot; &quot;);
    duk_insert(ctx, 0);
    duk_join(ctx, duk_get_top(ctx) - 1);
    fprintf(stdout, &quot;%s\\n&quot;, duk_to_string(ctx, -1));  /* &#39;stderr&#39; for alert() */
    fflush(stdout);  /* may or may not want to flush, depends on application */
    return 0;
}

/* And after Duktape heap creation (or after each new thread with a
 * fresh global environment):
 */

duk_push_c_function(ctx, my_print, DUK_VARARGS);
duk_put_global_string(ctx, &quot;print&quot;);
</code></pre></li><li><p>If you do need <code>print()</code> and/or <code>alert()</code> with the Duktape 1.x semantics you can include the following extra into your compilation: <code>extras/print-alert</code>.</p></li></ul><h3 id="built-in-commonjs-module-framework-removed" tabindex="-1">Built-in CommonJS module framework removed <a class="header-anchor" href="#built-in-commonjs-module-framework-removed" aria-label="Permalink to &quot;Built-in CommonJS module framework removed&quot;">​</a></h3><p>The built-in CommonJS module loading framework consisting of <code>require()</code>, <code>Duktape.modSearch()</code> and <code>Duktape.modLoaded</code> was removed; a module framework isn&#39;t always needed, and when it is, it&#39;s difficult for a single framework to match the very different use cases.</p><p>To upgrade:</p><ul><li>If you don&#39;t use the built-in module loading framework, no action is needed.</li><li>If you do use the built-in module loading framework and want to continue using a module loader with Duktape 1.x semantics, you can include the following extra into your compilation: <code>extras/module-duktape</code>.</li><li>If you&#39;re upgrading, there are also other alternatives to module loading. For example, the <code>extras/module-node</code> module loader provides Node.js-like semantics with a more flexible module resolution and loading process.</li></ul><h3 id="duktape-logger-duk-log-and-duk-log-va-removed" tabindex="-1">Duktape.Logger, duk_log(), and duk_log_va() removed <a class="header-anchor" href="#duktape-logger-duk-log-and-duk-log-va-removed" aria-label="Permalink to &quot;Duktape.Logger, duk_log(), and duk_log_va() removed&quot;">​</a></h3><p>The built-in logging framework consisting of <code>Duktape.Logger</code>, <code>duk_log()</code>, and <code>duk_log_va()</code> were removed because they depended on stdout/stderr which is a portability issue on some platforms. The logging framework also didn&#39;t always match user expectations: for some uses it was too simple (lacking e.g. expressive backend configuration); for other uses it was too complex (too high a ROM/RAM footprint for some embedded uses). Sometimes an existing API like <code>console.log()</code> was preferred while in other cases a platform specific logging binding was more appropriate.</p><p>To upgrade:</p><ul><li>If you don&#39;t need <code>Duktape.Logger</code> or the C logging API calls, no action is needed.</li><li>If you do need <code>Duktape.Logger</code> and/or the C logging API calls with Duktape 1.x semantics, you can include the following extra into your compilation: <code>extras/logging</code>.</li></ul><h3 id="duk-safe-call-userdata" tabindex="-1">duk_safe_call() userdata <a class="header-anchor" href="#duk-safe-call-userdata" aria-label="Permalink to &quot;duk_safe_call() userdata&quot;">​</a></h3><p>There&#39;s a new userdata argument for <code>duk_safe_call()</code>:</p><pre><code>/* Duktape 1.x */
typedef duk_ret_t (*duk_safe_call_function) (duk_context *ctx);
duk_int_t duk_safe_call(duk_context *ctx, duk_safe_call_function func, duk_idx_t nargs, duk_idx_t nrets);

/* Duktape 2.x */
typedef duk_ret_t (*duk_safe_call_function) (duk_context *ctx, void *udata);
duk_int_t duk_safe_call(duk_context *ctx, duk_safe_call_function func, void *udata, duk_idx_t nargs, duk_idx_t nrets);
</code></pre><p>The additional userdata argument makes it easier to pass a C pointer to the safe-called function without the need to push a pointer onto the value stack. Multiple C values can be passed by packing them into a stack-allocated struct and passing a pointer to the struct as the userdata.</p><p>To upgrade:</p><ul><li><p>Add a userdata argument to duk_safe_call() call sites. If no relevant userdata exists, pass a NULL.</p></li><li><p>Add a userdata argument to safe call targets. If no relevant userdata exists, just ignore the argument.</p></li><li><p>If a call site needs to support both Duktape 1.x and Duktape 2.x, use a DUK_VERSION preprocessor check:</p><pre><code>#if (DUK_VERSION &gt;= 20000)
duk_ret_t my_safe_call(duk_context *ctx, void *udata) {
#else
duk_ret_t my_safe_call(duk_context *ctx) {
#endif
    /* Ignore &#39;udata&#39;. */
}

/* ... */

#if (DUK_VERSION &gt;= 20000)
rc = duk_safe_call(ctx, my_safe_call, NULL, 1 /*nargs*/, 1 /*nrets*/);
#else
rc = duk_safe_call(ctx, my_safe_call, 1 /*nargs*/, 1 /*nrets*/);
#endif
</code></pre></li></ul><h3 id="duktape-specific-error-codes-removed-from-api" tabindex="-1">Duktape specific error codes removed from API <a class="header-anchor" href="#duktape-specific-error-codes-removed-from-api" aria-label="Permalink to &quot;Duktape specific error codes removed from API&quot;">​</a></h3><p>Duktape specific error codes were removed from the public API and from internals. These error codes were not very widely used, and they didn&#39;t have an ECMAScript counterpart (for example, a <code>DUK_ERR_API_ERROR</code> mapped to a plain <code>Error</code> object) which was confusing. The removed error codes and defines are:</p><ul><li><code>DUK_ERR_UNIMPLEMENTED_ERROR</code> / <code>DUK_RET_UNIMPLEMENTED_ERROR</code></li><li><code>DUK_ERR_UNSUPPORTED_ERROR</code> / <code>DUK_RET_UNSUPPORTED_ERROR</code></li><li><code>DUK_ERR_INTERNAL_ERROR</code> / <code>DUK_RET_INTERNAL_ERROR</code></li><li><code>DUK_ERR_ALLOC_ERROR</code> / <code>DUK_RET_ALLOC_ERROR</code></li><li><code>DUK_ERR_ASSERTION_ERROR</code> / <code>DUK_RET_ASSERTION_ERROR</code></li><li><code>DUK_ERR_API_ERROR</code> / <code>DUK_RET_API_ERROR</code></li><li><code>DUK_ERR_UNCAUGHT_ERROR</code> / <code>DUK_RET_UNCAUGHT_ERROR</code></li></ul><p>Duktape API related errors were also changed to map to either a <code>TypeError</code> or <code>RangeError</code> instead of a plain <code>Error</code>:</p><ul><li>A <code>RangeError</code> is used when an argument is out of bounds; for example: a value stack index is out of bounds, pop count is too large, not enough value stack items for call argument count.</li><li>A <code>TypeError</code> is used when a value has incorrect type, and is thrown by for example <code>duk_require_boolean()</code>. <code>TypeError</code> is also typically used when nothing else applies.</li></ul><p>To upgrade:</p><ul><li>If you use the custom error codes (<code>DUK_ERR_INTERNAL_ERROR</code> etc) in your code, convert to using standard error codes (<code>DUK_ERR_TYPE_ERROR</code>, etc).</li><li>If you depend on API errors mapping to a plain <code>Error</code>, revise such code to accept also <code>TypeError</code> or <code>RangeError</code>. (In general depending on a specific error type should be only be done when it&#39;s absolute necessary.)</li></ul><h3 id="duk-error-duk-error-va-duk-throw-duk-fatal-have-a-return-value" tabindex="-1">duk_error(), duk_error_va(), duk_throw(), duk_fatal() have a return value <a class="header-anchor" href="#duk-error-duk-error-va-duk-throw-duk-fatal-have-a-return-value" aria-label="Permalink to &quot;duk_error(), duk_error_va(), duk_throw(), duk_fatal() have a return value&quot;">​</a></h3><p>The prototype return value for these error throwers was changed from <code>void</code> to <code>duk_ret_t</code> which allows for idioms like:</p><pre><code>if (argvalue &lt; 0) {
    return duk_error(ctx, DUK_ERR_TYPE_ERROR,
                     &quot;invalid arg: %d&quot;, (int) argvalue);
}
</code></pre><p>To upgrade:</p><ul><li>Without an explicit cast to <code>(void) duk_error(...)</code> you may get some new compiler warnings. Fix by adding the void cast, or convert the call sites to use the <code>return duk_error(...)</code> idiom where applicable.</li></ul><h3 id="duk-dump-context-stdout-and-duk-dump-context-stderr-removed" tabindex="-1">duk_dump_context_stdout() and duk_dump_context_stderr() removed <a class="header-anchor" href="#duk-dump-context-stdout-and-duk-dump-context-stderr-removed" aria-label="Permalink to &quot;duk_dump_context_stdout() and duk_dump_context_stderr() removed&quot;">​</a></h3><p>These two API calls were helpers based on <code>duk_push_context_dump()</code> which would write the context dump directly to stdout/stderr. Having a dependency on stdout/stderr is a portability concern so the calls were removed in Duktape 2.x.</p><p>To upgrade:</p><ul><li><p>Replace <code>duk_dump_context_stdout()</code> with an explicit call sequence like:</p><pre><code>duk_push_context_dump(ctx);
printf(&quot;%s\\n&quot;, duk_to_string(ctx, -1));
duk_pop(ctx);
</code></pre><p>Similarly for <code>duk_dump_context_stderr()</code>.</p></li><li><p>Alternatively, include extras/duk-v1-compat into your compilation to add back the removed API calls.</p></li></ul><h3 id="duk-to-defaultvalue-removed" tabindex="-1">duk_to_defaultvalue() removed <a class="header-anchor" href="#duk-to-defaultvalue-removed" aria-label="Permalink to &quot;duk_to_defaultvalue() removed&quot;">​</a></h3><p>The <code>duk_to_defaultvalue()</code> API call was rather technical: it invoked the internal <code>[[DefaultValue]]</code> algorithm which is used in ES5.1 as part of the ToPrimitive() coercion (<code>duk_to_primitive()</code>). ES2015 no longer specifies <code>[[DefaultValue]]</code> which has been folded into ToPrimitive(). The API call thus no longer makes much sense.</p><p>To upgrade:</p><ul><li><p>If you&#39;re using <code>duk_to_defaultvalue()</code> (which is unlikely), you can in most cases replace it with <code>duk_to_primitive()</code>. The main difference is that <code>duk_to_primitive()</code> accepts all argument types (returning those considered primitive as is) while <code>duk_to_defaultvalue()</code> rejects primitive value arguments. See the ES5.1/ES2015 specifications for exact differences between the two.</p></li><li><p>Here&#39;s an example replacement. Replace this:</p><pre><code>duk_to_defaultvalue(ctx, idx, hint);
</code></pre><p>with:</p><pre><code>duk_require_type_mask(ctx, idx, DUK_TYPE_MASK_OBJECT |
                                DUK_TYPE_MASK_BUFFER |
                                DUK_TYPE_MASK_LIGHTFUNC);
duk_to_primitive(ctx, idx, hint);
</code></pre></li><li><p>Alternatively, include extras/duk-v1-compat into your compilation to add back the removed API call.</p></li></ul><h3 id="file-i-o-duktape-c-api-calls-were-removed" tabindex="-1">File I/O Duktape C API calls were removed <a class="header-anchor" href="#file-i-o-duktape-c-api-calls-were-removed" aria-label="Permalink to &quot;File I/O Duktape C API calls were removed&quot;">​</a></h3><p>Some platform don&#39;t have file I/O API calls (even ANSI), while on others they are present but don&#39;t actually map to the file system (instead, a platform specific API is used to access the actual file system). Finally, there are character encoding issues with ANSI C file I/O APIs e.g. on Windows, so that the built-in file I/O support didn&#39;t always work as expected.</p><p>To improve portability, the following Duktape C API calls depending on platform file I/O (fopen() etc) were removed (moved to extras):</p><ul><li>duk_push_string_file()</li><li>duk_compile_file()</li><li>duk_pcompile_file()</li><li>duk_eval_file()</li><li>duk_eval_file_noresult()</li><li>duk_peval_file()</li><li>duk_peval_file_noresult()</li></ul><p>To upgrade:</p><ul><li>If you don&#39;t use these API calls, no action is needed.</li><li>If you use these API calls you can e.g. implement a helper to push a file as a string (like <code>duk_push_string_file()</code>) and then implement any needed compile/eval helpers based on that.</li><li>Alternatively, you can include the following extra into your compilation: <code>extras/duk-v1-compat</code>. The extra provides Duktape 1.x compatible file-related API call bindings.</li></ul><h3 id="duk-debugger-attach-and-duk-debugger-attach-custom-merged" tabindex="-1">duk_debugger_attach() and duk_debugger_attach_custom() merged <a class="header-anchor" href="#duk-debugger-attach-and-duk-debugger-attach-custom-merged" aria-label="Permalink to &quot;duk_debugger_attach() and duk_debugger_attach_custom() merged&quot;">​</a></h3><p>The <code>duk_debugger_attach_custom()</code> API call in Duktape 1.x has been renamed to <code>duk_debugger_attach()</code> to eliminate an unnecessary API call variant from the public API. The remaining debugger attach call always includes an AppRequest callback argument.</p><p>To upgrade:</p><ul><li><p><code>duk_debugger_attach_custom()</code> call sites: rename API call to <code>duk_debugger_attach()</code>; no argument changes are needed.</p></li><li><p><code>duk_debugger_attach()</code> call sites: add a NULL <code>request_cb</code> callback argument.</p></li><li><p>If a call site needs to support both Duktape 1.x and Duktape 2.x:</p><pre><code>/* Alternative #1: conditional call name. */
#if (DUK_VERSION &gt;= 20000)
    duk_debugger_attach(
#else
    duk_debugger_attach_custom(
#endif
        read_cb,
        write_cb,
        peek_cb,
        read_flush_cb,
        write_flush_cb,
        request_cb,  /* NULL OK if not necessary */
        detached_cb,
        udata);

/* Alternative #2: conditional request_cb argument. */
    duk_debugger_attach(
        read_cb,
        write_cb,
        peek_cb,
        read_flush_cb,
        write_flush_cb,
#if (DUK_VERSION &gt;= 20000)
        request_cb,  /* NULL OK if not necessary */
#endif
        detached_cb,
        udata);
</code></pre></li></ul><h3 id="debug-protocol-version-bumped-from-1-to-2" tabindex="-1">Debug protocol version bumped from 1 to 2 <a class="header-anchor" href="#debug-protocol-version-bumped-from-1-to-2" aria-label="Permalink to &quot;Debug protocol version bumped from 1 to 2&quot;">​</a></h3><p>Because there are small incompatible changes in the debug protocol in this release, the debug protocol version has been bumped from 1 to 2. The version is provided by the <code>DUK_DEBUG_PROTOCOL_VERSION</code> constant, and also appears in the debug protocol version identification string.</p><p>To upgrade:</p><ul><li>Review the debug protocol changes and ensure debug client has corresponding changes.</li><li>Update debug client code to support both versions 1 and 2, or version 2 only.</li></ul><h3 id="debugger-detached-callback-has-a-duk-context-pointer-argument" tabindex="-1">Debugger detached callback has a duk_context pointer argument <a class="header-anchor" href="#debugger-detached-callback-has-a-duk-context-pointer-argument" aria-label="Permalink to &quot;Debugger detached callback has a duk_context pointer argument&quot;">​</a></h3><p>The debugger detached callback is allowed to immediately reattach the debugger session. However, the detached callback didn&#39;t have a <code>duk_context *</code> argument in Duktape 1.x so that the relevant context pointer needed to be passed e.g. via the udata argument which is awkward.</p><p>In Duktape 2.x an explicit context argument was added:</p><pre><code>/* Duktape 1.x */
typedef void (*duk_debug_detached_function) (void *udata);

/* Duktape 2.x */
typedef void (*duk_debug_detached_function) (duk_context *ctx, void *udata);
</code></pre><p>To upgrade:</p><ul><li><p>If you&#39;re using <code>duk_debugger_attach()</code>, add an additional <code>duk_context *</code> argument to the detached callback.</p></li><li><p>If support for both Duktape 1.x and 2.x is desired, use:</p><pre><code>#if DUK_VERSION &gt;= 20000
void my_detached_cb(duk_context *ctx, void *udata) {
#else
void my_detached_cb(void *udata) {
#end
    /* ... */
}
</code></pre></li></ul><h3 id="debugger-command-callstack-index-changes" tabindex="-1">Debugger command callstack index changes <a class="header-anchor" href="#debugger-command-callstack-index-changes" aria-label="Permalink to &quot;Debugger command callstack index changes&quot;">​</a></h3><p>Debug command callstack indexes have been made mandatory where appropriate to simplify the protocol. Affected commands are: GetVar, PutVar, GetLocals, and Eval.</p><p>To upgrade:</p><ul><li>Review debug client handling of callstack indices when sending affected commands.</li></ul><h3 id="debugger-print-alert-and-logger-forwarding-removed" tabindex="-1">Debugger print/alert and logger forwarding removed <a class="header-anchor" href="#debugger-print-alert-and-logger-forwarding-removed" aria-label="Permalink to &quot;Debugger print/alert and logger forwarding removed&quot;">​</a></h3><p>Forwarding of <code>print()</code>, <code>alert()</code>, and log writes, enabled using config options <code>DUK_USE_DEBUGGER_FWD_PRINTALERT</code> and <code>DUK_USE_DEBUGGER_FWD_LOGGING</code>, was removed as part of removing the bindings themselves. Also debugger notifications Print (0x02), Alert (0x03), Log (0x04) were deprecated.</p><p>To upgrade:</p><ul><li>No changes are needed, but print/alert and logger notification support can be removed from a debug client.</li><li>If you rely on print/alert or logger forwarding in your debugger setup, you can add custom print/alert or logger forwarding by implementing print/alert or logging yourself and using AppNotify (<code>duk_debugger_notify()</code>) to forward print/alert or logger text.</li></ul><h3 id="internal-duk-harray-affects-debugger-array-inspection" tabindex="-1">Internal duk_harray affects debugger array inspection <a class="header-anchor" href="#internal-duk-harray-affects-debugger-array-inspection" aria-label="Permalink to &quot;Internal duk_harray affects debugger array inspection&quot;">​</a></h3><p>Duktape 2.x introduces an internal <code>duk_harray</code> type to represent arrays. The array <code>.length</code> property is no longer stored in the property table of the array but is a C struct field in <code>duk_harray</code> and the property visible to ECMAScript code is virtual.</p><p>As a result, array <code>.length</code> is not visible when inspecting ordinary array properties using e.g. GetObjPropDesc or GetObjPropDescRange. Instead, array <code>.length</code> is an artificial property <code>&quot;length&quot;</code> returned by GetHeapObjInfo.</p><p>To upgrade:</p><ul><li>If the debug client uses array <code>.length</code> for e.g. UI purposes, ensure the artificial property <code>&quot;length&quot;</code> is used instead.</li></ul><h3 id="other-debugger-changes" tabindex="-1">Other debugger changes <a class="header-anchor" href="#other-debugger-changes" aria-label="Permalink to &quot;Other debugger changes&quot;">​</a></h3><ul><li>Artificial properties renamed for consistency with internal renaming: <ul><li><code>compiledfunction</code> -&gt; <code>compfunc</code></li><li><code>nativefunction</code> -&gt; <code>natfunc</code></li><li><code>bufferobject</code> -&gt; <code>bufobj</code></li><li><code>bound</code> -&gt; <code>boundfunc</code></li></ul></li></ul><h3 id="debug-print-config-options-changed" tabindex="-1">Debug print config options changed <a class="header-anchor" href="#debug-print-config-options-changed" aria-label="Permalink to &quot;Debug print config options changed&quot;">​</a></h3><p>Debug print related config options were reworked as follows:</p><ul><li><p>Debug prints no longer automatically go to <code>stderr</code>. Instead, an application must define <code>DUK_USE_DEBUG_WRITE()</code> in <code>duk_config.h</code> when <code>DUK_USE_DEBUG</code> is enabled. The macro is called to write debug log lines; there&#39;s no default provider to avoid platform I/O dependencies. Using a user-provided macro removes a dependency on platform I/O and also allows debug logs to be filtered and redirected in whatever manner is most useful for the application. Example provider:</p><pre><code>#define DUK_USE_DEBUG_WRITE(level,file,line,func,msg) do { \\
        fprintf(stderr, &quot;D%ld %s:%ld (%s): %s\\n&quot;, \\
                (long) (level), (file), (long) (line), (func), (msg)); \\
    } while (0)
</code></pre><p>See <a href="http://wiki.duktape.org/HowtoDebugPrints.html" target="_blank" rel="noreferrer">http://wiki.duktape.org/HowtoDebugPrints.html</a> for more information.</p></li><li><p>Debug level options <code>DUK_USE_DPRINT</code>, <code>DUK_USE_DDPRINT</code>, and <code>DUK_DDDPRINT</code> were replaced with a single config option <code>DUK_USE_DEBUG_LEVEL</code> with a numeric value:</p><ul><li>0 is minimal logging (matches <code>DUK_USE_DPRINT</code>)</li><li>1 is verbose logging (matches <code>DUK_USE_DDPRINT</code>)</li><li>2 is very verbose logging (matches <code>DUK_USE_DDDPRINT</code>)</li></ul></li></ul><p>To upgrade:</p><ul><li>If you&#39;re not using debug prints, no action is needed.</li><li>If you&#39;re using debug prints: <ul><li>Add a <code>DUK_USE_DEBUG_WRITE()</code> to your <code>duk_config.h</code>. By itself it won&#39;t enable debug prints so it&#39;s safe to add even when debug prints are disabled.</li><li>Convert debug level options from <code>DUK_USE_{D,DD,DDD}PRINT</code> to the equivalent <code>DUK_USE_DEBUG_LEVEL</code> (0, 1, or 2).</li></ul></li></ul><h3 id="fatal-error-and-panic-handling-reworked" tabindex="-1">Fatal error and panic handling reworked <a class="header-anchor" href="#fatal-error-and-panic-handling-reworked" aria-label="Permalink to &quot;Fatal error and panic handling reworked&quot;">​</a></h3><p>The following changes were made to fatal error and panic handling:</p><ul><li><p>Fatal error function signature was simplied from:</p><pre><code>/* Duktape 1.x */
void func(duk_context *ctx, duk_errcode_t code, const char *msg);
</code></pre><p>to:</p><pre><code>/* Duktape 2.x */
void func(void *udata, const char *msg);
</code></pre><p>where the <code>udata</code> argument is the userdata argument given in heap creation.</p></li><li><p><code>duk_fatal()</code> error code argument was removed to match the signature change.</p></li><li><p>The entire concept of &quot;panic errors&quot; was removed and replaced with calls to the fatal error mechanism. There&#39;s a user-registered (optional) fatal error handler in heap creation, and a built-in default fatal error handler which is called if user code doesn&#39;t provide a fatal error handler.</p><p>Some fatal errors, currently assertion failures, happen without a Duktape heap/thread context so that a user-registered handler cannot be called (there&#39;s no heap reference to look it up). For these errors the default fatal error handler is always called, with the userdata argument as <code>NULL</code>. The default fatal error handler can be replaced by editing <code>duk_config.h</code>.</p></li></ul><p>To upgrade:</p><ul><li><p>If you&#39;re not providing a fatal error handler nor using a custom panic handler, no action is needed -- however, providing a fatal error handler in heap creation is <strong>strongly recommended</strong>, see <a href="http://wiki.duktape.org/HowtoFatalErrors.html" target="_blank" rel="noreferrer">http://wiki.duktape.org/HowtoFatalErrors.html</a> for instructions.</p><p>The default fatal error handler will by default call <code>abort()</code> with no error message to <code>stdout</code> or <code>stderr</code>. To improve this behavior define <code>DUK_USE_FATAL_HANDLER()</code> in your <code>duk_config.h</code>.</p></li><li><p>If you have a fatal error handler, update its signature:</p><pre><code>/* Duktape 1.x */
void my_fatal(duk_context *ctx, duk_errcode_t error_code, const char *msg) {
    /* ... */
}

/* Duktape 2.x */
void my_fatal(void *udata, const char *msg) {
    /* ... */
}
</code></pre></li><li><p>If you&#39;re using <code>duk_fatal()</code> API calls, remove the error code argument:</p><pre><code>/* Duktape 1.x */
duk_fatal(ctx, DUK_ERR_INTERNAL_ERROR, &quot;assumption failed&quot;);

/* Duktape 2.x */
duk_fatal(ctx, &quot;assumption failed&quot;);
</code></pre></li><li><p>If you have a custom panic handler in your <code>duk_config.h</code>, convert it to a default fatal error handler, also provided by <code>duk_config.h</code>. Both Duktape 1.x panic handler and Duktape 2.x default fatal error handler apply to all Duktape heaps (rather than a specific Duktape heap).</p></li></ul><h3 id="initjs-support-removed" tabindex="-1">InitJS support removed <a class="header-anchor" href="#initjs-support-removed" aria-label="Permalink to &quot;InitJS support removed&quot;">​</a></h3><p>Both Duktape InitJS (<code>DUK_USE_BUILTIN_INITJS</code>) and user InitJS (<code>DUK_USE_USER_INITJS</code>) were removed. Duktape built-in InitJS is no longer needed (and was never used for very much). User InitJS was rarely used and it&#39;s not a full solution because custom environment initialization may also involve native initialization code which isn&#39;t supported by the mechanism.</p><p>To upgrade:</p><ul><li>Duktape built-in InitJS removal requires no user code changes.</li><li>If you&#39;re using the user InitJS option, call sites need to be modified to run the init code explicitly on heap/thread creation.</li></ul><h3 id="enumeration-order-changes" tabindex="-1">Enumeration order changes <a class="header-anchor" href="#enumeration-order-changes" aria-label="Permalink to &quot;Enumeration order changes&quot;">​</a></h3><p>Enumeration order for <code>Object.getOwnPropertyNames()</code> has been changed to match ES2015/ES2016 <code>[[OwnPropertyKeys]]</code> enumeration order, which is:</p><ul><li>Array indices in ascending order</li><li>Normal (non-array-index) property keys in insertion order</li><li>Symbols in insertion order</li></ul><p>While not required by ES2015/ES2016, the same enumeration order is also used in Duktape 2.x for <code>for-in</code>, <code>Object.keys()</code>, and <code>duk_enum()</code>. A related change is that <code>duk_enum()</code> flags <code>DUK_ENUM_ARRAY_INDICES_ONLY</code> and <code>DUK_ENUM_SORT_ARRAY_INDICES</code> can now be used independently.</p><p>The revised enumeration order makes enumeration behavior more predictable and matches other modern engines. In particular, sparse arrays (arrays without an internal array part) now enumerate identically to dense arrays.</p><p>To upgrade:</p><ul><li>Check application code for enumeration assumptions.</li></ul><h3 id="symbol-support-related-changes" tabindex="-1">Symbol support related changes <a class="header-anchor" href="#symbol-support-related-changes" aria-label="Permalink to &quot;Symbol support related changes&quot;">​</a></h3><p>Small changes related to adding symbol support:</p><ul><li>Symbols are represented as strings with an invalid UTF-8 representation (like internal keys in Duktape 1.x), and they behave like strings in the C API just like internal keys did in Duktape 1.x. For example, <code>duk_is_string()</code> is true for symbols. Symbols can be distinguished using <code>duk_is_symbol()</code>.</li><li>Symbol support is currently <strong>experimental</strong>. The core semantics have been implemented but it&#39;s likely some of the internal details will change in future releases. The C API may also need changes (for example to the typing model) in future releases; in its current form symbols behave just like internal strings in the C API.</li><li>Being experimental, the <code>Symbol</code> built-in is disabled by default; enable via config option <code>DUK_USE_SYMBOL_BUILTIN</code>. Symbols can be created from C code even when the <code>Symbol</code> built-in is disabled, and symbol semantics (like <code>typeof</code> and coercion behavior) are currently enabled.</li><li>Internal properties are now called &quot;hidden symbols&quot; and adopt some ES2015 Symbol behaviors, e.g. <code>typeof internalKey === &#39;symbol</code> and <code>&quot;&quot; + internalKey</code> is now a TypeError. Internal keys are different from normal ES2015 Symbols in that they can&#39;t be enumerated from ECMAScript code even with <code>Object.getOwnPropertySymbols()</code>.</li><li>The <code>DUK_ENUM_INCLUDE_INTERNAL</code> C API flag has been renamed <code>DUK_ENUM_INCLUDE_HIDDEN</code>.</li></ul><h3 id="other-incompatible-changes" tabindex="-1">Other incompatible changes <a class="header-anchor" href="#other-incompatible-changes" aria-label="Permalink to &quot;Other incompatible changes&quot;">​</a></h3><p>Incompatible changes (not exhaustive, also see RELEASES.rst):</p><ul><li>Normal and constructor function call argument limit is now 255, down from the previous 511.</li><li>If a user function is called using the identifier &#39;eval&#39;, such a call won&#39;t get tailcall optimized even if otherwise possible.</li><li><code>duk_gc()</code> no longer accepts a NULL context pointer for consistence with other API calls. A NULL pointer not causes memory unsafe behavior, as with all other API calls.</li><li><code>duk_def_prop()</code> now ToPropertyKey() coerces its argument rather than requiring the key to be a string. This allows e.g. numbers to be used as property keys.</li><li><code>duk_char_code_at()</code> and <code>String.charCodeAt()</code> now return 0xFFFD (Unicode replacement character) if the string cannot be decoded as extended UTF-8, previously an error was thrown. This situation never occurs for standard ECMAScript strings or valid UTF-8 strings.</li><li><code>duk_get_length()</code> now allows the <code>size_t</code> rather than the unsigned 32-bit integer range for the target value&#39;s <code>.length</code>.</li><li>Legacy octal literal handling has been improved to match more closely with ES2015 Annex B. Octal look-alike decimal literals like &quot;0778&quot; and &quot;0778.123&quot; are now allowed.</li><li>Legacy octal escape handling in string literals has been improved to match more closely with ES2015 Annex B and other engines: &quot;078&quot; is not accepted and is the same as &quot;u00078&quot;, &quot;8&quot; and &quot;9&quot; are accepted as literal &quot;8&quot; and &quot;9&quot; (even in strict mode).</li><li>The NetBSD pow() workaround option <code>DUK_USE_POW_NETBSD_WORKAROUND</code> has been generalized and renamed to <code>DUK_USE_POW_WORKAROUNDS</code>.</li><li>When using a Proxy as a for-in target the &quot;ownKeys&quot; trap is invoked instead of the &quot;enumerate&quot; trap in ES2016. Duktape now follows this behavior. The &quot;enumerate&quot; trap has been obsoleted. Key enumerability is also now checked when &quot;ownKeys&quot; trap is used in Object.keys() and for-in.</li><li>Bound function internal prototype is copied from the target function to match ES2015 requirements; in ES5 (and Duktape 1.x) bound function internal prototype is always set to Function.prototype.</li><li>Function.prototype.toString() output has been changed to match ES2015 requirements. For example <code>function foo() {&quot;ecmascript&quot;}</code> is now <code>function foo() { [ecmascript code] }</code>.</li><li>Function <code>.name</code> and <code>.length</code> properties are now non-writable, non-enumerable, but configurable, to match revised ES2015 semantics. Previously the properties were also non-configurable.</li><li>Function <code>.fileName</code> property is now non-writable, non-enumerable, and configurable. Previously it was writable, non-enumerable, and configurable. While this is not required by ES2015 (as the property is non-standard), it has been aligned with <code>.name</code>. Direct assignment to the property will be rejected, but you can set it using e.g. <code>Object.defineProperty(func, &#39;fileName&#39;, { value: &#39;myFilename.js&#39; });</code>.</li><li>Error instance <code>.fileName</code> and <code>.lineNumber</code> property attributes are also non-writable, non-enumerable, but configurable. This only matters when tracebacks are disabled and concrete properties are used instead of the inherited accessors.</li><li>Object constructor methods like Object.keys(), Object.freeze(), etc now follow more lenient ES2015 coercion semantics: non-object arguments are either coerced to objects or treated like non-extensible objects with no own properties.</li><li>RegExp.prototype follows ES2015 behavior more closely: it is no longer a RegExp instance, .source, .global, .ignoreCase, and .multiline are now inherited getters, etc. However, leniency to allow e.g. RegExp.prototype.source (from ES2017 draft) is supported for real world code compatibility.</li><li>Duktape.info() now returns an object rather than an array. The object has named properties like <code>.type</code> and <code>.enext</code> for the internal fields which is easier to version and work with. The names of the properties are not under version guarantees and may change in an incompatible fashion in even a minor release.</li><li>Memory management without mark-and-sweep is no longer supported; relying on only refcounting was error prone because reference cycles or debugger use could result in leaked garbage that would only get collected on heap destruction.</li></ul><h2 id="known-issues" tabindex="-1">Known issues <a class="header-anchor" href="#known-issues" aria-label="Permalink to &quot;Known issues&quot;">​</a></h2><ul><li>Some non-compliant behavior for array indices near 2G or 4G elements.</li><li>RegExp parser is strict and won&#39;t accept some real world RegExps which are technically not compliant with ECMAScript E5/E5.1 specification but allowed in ES2015 Annex B.</li><li>Final mantissa bit rounding issues in the internal number-to-string conversion.</li></ul><h2 id="raw-issues-from-test-runs" tabindex="-1">Raw issues from test runs <a class="header-anchor" href="#raw-issues-from-test-runs" aria-label="Permalink to &quot;Raw issues from test runs&quot;">​</a></h2><h3 id="api-tests" tabindex="-1">API tests <a class="header-anchor" href="#api-tests" aria-label="Permalink to &quot;API tests&quot;">​</a></h3><pre><code>test-to-number.c: fail; 15 diff lines; known issue: number parsing bug for strings containing NUL characters (e.g. &#39;\\u0000&#39;)
</code></pre><h3 id="ecmascript-tests" tabindex="-1">ECMAScript tests <a class="header-anchor" href="#ecmascript-tests" aria-label="Permalink to &quot;ECMAScript tests&quot;">​</a></h3><pre><code>test-bi-array-proto-push: fail; 30 diff lines; known issue: array length above 2^32-1 not supported
test-bi-array-push-maxlen: fail; 17 diff lines; known issue: array length above 2^32-1 not supported
test-bi-date-tzoffset-brute-fi: fail; 12 diff lines; known issue: year 1970 deviates from expected, Duktape uses equiv. year for 1970 on purpose at the moment; requires special feature options: test case has been written for Finnish locale
test-bi-function-nonstd-caller-prop: fail; 175 diff lines; requires special feature options: DUK_USE_NONSTD_FUNC_CALLER_PROPERTY
test-bi-global-parseint: fail; 89 diff lines; known issue: rounding differences for parsing integers larger than 2^53
test-bi-json-enc-proplist-dups: fail; 8 diff lines; known issue: JSON.stringify() can be given a property list to serialize; duplicates should be eliminated but Duktape (and other engines) will happily serialize a property multiple times
test-bi-json-enc-proxy: fail; 13 diff lines; known issue: JSON enumeration behavior for Proxy targets is incomplete and uses &#39;enumerate&#39; trap instead of &#39;ownKeys&#39; trap
test-bi-number-proto-toexponential: fail; 75 diff lines; known issue: corner case rounding errors in toExponential()
test-bi-number-proto-tostring: fail; 46 diff lines; known issue: expect strings to be checked, but probably Duktape rounding issues
test-bi-proxy-object-tostring: fail; 6 diff lines; known issue: Object class handling for Proxy objects is incomplete
test-bi-regexp-gh39: fail; 7 diff lines; known issue: requires leniency for non-standard regexps
test-bi-symbol-coercion: fail; 9 diff lines; known issue: no @@toPrimitive coercion yet
test-bi-typedarray-misc-inherited-accessors: fail; 21 diff lines; known issue: typed array .length etc not yet inherited accessors (ES2015 requirement)
test-bug-date-timeval-edges: fail; 17 diff lines; known issue: test case depends on current timezone offset
test-bug-enum-shadow-nonenumerable: fail; 12 diff lines; known issue: corner case enumeration semantics, not sure what correct behavior is (test262 ch12/12.6/12.6.4/12.6.4-2)
test-bug-json-parse-__proto__: fail; 18 diff lines; known issue: when ES2015 __proto__ enabled, JSON.parse() parses &#39;__proto__&#39; property incorrectly when a specially crafted reviver is used
test-bug-numconv-1e23: fail; 10 diff lines; known issue: corner case in floating point parse rounding
test-bug-numconv-denorm-toprec: fail; 7 diff lines; known issue: in a denormal corner case toPrecision() can output a zero leading digit
test-bug-tonumber-u0000: fail; 7 diff lines; known issue: &#39;\\u0000&#39; should ToNumber() coerce to NaN, but now coerces to zero like an empty string
test-dev-16bit-overflows: fail; 11 diff lines; requires special feature options: requires 16-bit field options
test-dev-func-cons-args: fail; 18 diff lines; known issue: corner cases for &#39;new Function()&#39; when arguments and code are given as strings
test-dev-func-varmap-drop: fail; 46 diff lines; requires special feature options: debugger support must be disabled
test-dev-lightfunc-accessor: fail; 50 diff lines; requires special feature options: DUK_USE_LIGHTFUNC_BUILTINS
test-dev-lightfunc-finalizer: fail; 8 diff lines; requires special feature options: DUK_USE_LIGHTFUNC_BUILTINS
test-dev-lightfunc: fail; 518 diff lines; requires special feature options: DUK_USE_LIGHTFUNC_BUILTINS
test-dev-object-literal-method-computed: fail; 8 diff lines; known issue: computed name for object literal method shorthand not yet implemented
test-dev-yield-after-callapply: fail; 8 diff lines; known issue: yield() not allowed when function called via Function.prototype.(call|apply)()
test-lex-unterminated-hex-uni-escape: fail; 29 diff lines; known issue: unterminated hex escapes should be parsed leniently, e.g. &#39;\\uX&#39; -&gt; &#39;uX&#39; but Duktape now refuses to parse them
test-numconv-parse-misc: fail; 12 diff lines; known issue: rounding corner case for 1e+23 (parses/prints as 1.0000000000000001e+23)
test-numconv-tostring-gen: fail; 257 diff lines; known issue: rounding corner cases in number-to-string coercion
test-numconv-tostring-misc: fail; 6 diff lines; known issue: rounding corner case, 1e+23 string coerces to 1.0000000000000001e+23
test-regexp-empty-quantified: fail; 16 diff lines; known issue: a suitable empty quantified (e.g. &#39;(x*)*&#39;) causes regexp parsing to terminate due to step limit
test-regexp-invalid-charclass: fail; 7 diff lines; known issue: some invalid character classes are accepted (e.g. &#39;[\\d-z]&#39; and &#39;[z-x]&#39;)
test-stmt-for-in-lhs: fail; 29 diff lines; known issue: for-in allows some invalid left-hand-side expressions which cause a runtime ReferenceError instead of a compile-time SyntaxError (e.g. &#39;for (a+b in [0,1]) {...}&#39;)
</code></pre><h3 id="test262" tabindex="-1">test262 <a class="header-anchor" href="#test262" aria-label="Permalink to &quot;test262&quot;">​</a></h3><pre><code>ch12/12.6/12.6.4/12.6.4-2 in non-strict mode   // diagnosed: enumeration corner case issue, see test-bug-enum-shadow-nonenumerable.js
ch15/15.10/15.10.2/15.10.2.5/S15.10.2.5_A1_T5 in non-strict mode   // diagnosed: Duktape bug, matching /(a*)b\\1+/ against &#39;baaaac&#39; causes first capture to match the empty string; the &#39;\\1+&#39; part will then use the &#39;+&#39; quantifier over the empty string.  As there is no handling to empty quantified now, Duktape bails out with a RangeError.
ch15/15.10/15.10.2/15.10.2.9/S15.10.2.9_A1_T5 in non-strict mode   // diagnosed: Duktape bug, matching /(a*)b\\1+/ against &#39;baaac&#39; causes first capture to be empty, the &#39;\\1+&#39; part will then quantify over an empty string leading to Duktape RangeError (there is no proper handling for an empty quantified now)
ch15/15.4/15.4.4/15.4.4.10/S15.4.4.10_A3_T3 in non-strict mode   // diagnosed: probably Duktape bug related to long array corner cases or &#39;length&#39; sign handling (C typing?)
ch15/15.4/15.4.4/15.4.4.12/S15.4.4.12_A3_T3 in non-strict mode   // diagnosed: probably Duktape bug related to long array corner cases or &#39;length&#39; sign handling (C typing?)
ch15/15.4/15.4.4/15.4.4.14/15.4.4.14-5-12 in non-strict mode   // diagnosed: Array length over 2G, not supported right now
ch15/15.4/15.4.4/15.4.4.14/15.4.4.14-5-16 in non-strict mode   // diagnosed: Array length over 2G, not supported right now
ch15/15.4/15.4.4/15.4.4.14/15.4.4.14-9-9 in non-strict mode   // diagnosed: a.indexOf(&lt;n&gt;,4294967290) returns -1 for all indices n=2,3,4,5 but is supposed to return 4294967294 for n=2.  The cause is long array corner case handling, possibly signed length handling (C typing?)
ch15/15.4/15.4.4/15.4.4.15/15.4.4.15-5-12 in non-strict mode   // diagnosed: probably Duktape bug: long array corner cases (C typing?)
ch15/15.4/15.4.4/15.4.4.15/15.4.4.15-5-16 in non-strict mode   // diagnosed: probably Duktape bug: long array corner cases (C typing?)
ch15/15.4/15.4.4/15.4.4.15/15.4.4.15-8-9 in non-strict mode   // diagnosed: probably Duktape bug: long array corner cases (C typing?)
ch15/15.4/15.4.4/15.4.4.6/S15.4.4.6_A4_T1 in non-strict mode   // diagnosed: Array .pop() fast path (can be disabled) ignores inherited array index properties
</code></pre>`,170)]))}const f=o(i,[["render",r]]);export{h as __pageData,f as default};
