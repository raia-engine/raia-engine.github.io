import{_ as s,c as n,a2 as e,o as t}from"./chunks/framework.CKGreiRV.js";const u=JSON.parse('{"title":"ステートメント","description":"","frontmatter":{},"headers":[],"relativePath":"resource/swift/statements.md","filePath":"resource/swift/statements.md","lastUpdated":1735910155000}'),i={name:"resource/swift/statements.md"};function p(l,a,o,c,r,d){return t(),n("div",null,a[0]||(a[0]=[e(`<h1 id="ステートメント" tabindex="-1">ステートメント <a class="header-anchor" href="#ステートメント" aria-label="Permalink to &quot;ステートメント&quot;">​</a></h1><p>式をグループ化し、実行の流れを制御します。Swiftには、単純ステートメント、コンパイラ制御ステートメント、および制御フローステートメントの3種類のステートメントがあります。単純ステートメントは最も一般的で、式または宣言のいずれかで構成されます。コンパイラ制御ステートメントは、プログラムがコンパイラの動作の側面を変更できるようにし、条件付きコンパイルブロックと行制御ステートメントを含みます。</p><p>制御フローステートメントは、プログラム内の実行の流れを制御するために使用されます。Swiftには、ループステートメント、分岐ステートメント、および制御転送ステートメントなど、いくつかの種類の制御フローステートメントがあります。ループステートメントは、指定された条件に応じてコードのブロックを繰り返し実行することを可能にし、分岐ステートメントは、特定の条件が満たされた場合にのみコードのブロックを実行することを可能にし、制御転送ステートメントは、コードの実行順序を変更する方法を提供します。さらに、Swiftはスコープを導入し、エラーをキャッチして処理するためのdoステートメントと、現在のスコープが終了する直前にクリーンアップアクションを実行するためのdeferステートメントを提供します。</p><p>ステートメントの後にはオプションでセミコロン（;）を付けることができ、同じ行に複数のステートメントがある場合はそれらを区切るために使用されます。</p><h2 id="ステートメントの文法" tabindex="-1">ステートメントの文法 <a class="header-anchor" href="#ステートメントの文法" aria-label="Permalink to &quot;ステートメントの文法&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>statement → expression ;?</span></span>
<span class="line"><span>statement → declaration ;?</span></span>
<span class="line"><span>statement → loop-statement ;?</span></span>
<span class="line"><span>statement → branch-statement ;?</span></span>
<span class="line"><span>statement → labeled-statement ;?</span></span>
<span class="line"><span>statement → control-transfer-statement ;?</span></span>
<span class="line"><span>statement → defer-statement ;?</span></span>
<span class="line"><span>statement → do-statement ;?</span></span>
<span class="line"><span>statement → compiler-control-statement</span></span>
<span class="line"><span>statements → statement statements?</span></span></code></pre></div><h2 id="ループステートメント" tabindex="-1">ループステートメント <a class="header-anchor" href="#ループステートメント" aria-label="Permalink to &quot;ループステートメント&quot;">​</a></h2><p>ループステートメントは、ループ内で指定された条件に応じてコードのブロックを繰り返し実行することを可能にします。Swiftには、for-inステートメント、whileステートメント、およびrepeat-whileステートメントの3つのループステートメントがあります。</p><p>ループステートメント内の制御フローは、breakステートメントおよびcontinueステートメントによって変更されることがあり、以下のBreak StatementおよびContinue Statementで説明されています。</p><h3 id="ループステートメントの文法" tabindex="-1">ループステートメントの文法 <a class="header-anchor" href="#ループステートメントの文法" aria-label="Permalink to &quot;ループステートメントの文法&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>loop-statement → for-in-statement</span></span>
<span class="line"><span>loop-statement → while-statement</span></span>
<span class="line"><span>loop-statement → repeat-while-statement</span></span></code></pre></div><h3 id="for-inステートメント" tabindex="-1">For-Inステートメント <a class="header-anchor" href="#for-inステートメント" aria-label="Permalink to &quot;For-Inステートメント&quot;">​</a></h3><p>for-inステートメントは、コレクション（またはSequenceプロトコルに準拠する任意の型）の各アイテムに対して一度だけコードのブロックを実行することを可能にします。</p><p>for-inステートメントは次の形式を持ちます：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>for &lt;#item#&gt; in &lt;#collection#&gt; {</span></span>
<span class="line"><span>   &lt;#statements#&gt;</span></span>
<span class="line"><span>}</span></span></code></pre></div><p><code>makeIterator()</code>メソッドは、コレクション式で呼び出され、イテレータ型の値を取得します。プログラムは、イテレータの<code>next()</code>メソッドを呼び出すことでループの実行を開始します。返された値がnilでない場合、それはアイテムパターンに割り当てられ、プログラムはステートメントを実行し、ループの先頭に戻ります。そうでない場合、プログラムは割り当てやステートメントの実行を行わず、for-inステートメントの実行を終了します。</p><h4 id="for-inステートメントの文法" tabindex="-1">For-Inステートメントの文法 <a class="header-anchor" href="#for-inステートメントの文法" aria-label="Permalink to &quot;For-Inステートメントの文法&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>for-in-statement → for case? pattern in expression where-clause? code-block</span></span></code></pre></div><h3 id="whileステートメント" tabindex="-1">Whileステートメント <a class="header-anchor" href="#whileステートメント" aria-label="Permalink to &quot;Whileステートメント&quot;">​</a></h3><p>whileステートメントは、条件が真である限り、コードのブロックを繰り返し実行することを可能にします。</p><p>whileステートメントは次の形式を持ちます：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>while &lt;#condition#&gt; {</span></span>
<span class="line"><span>   &lt;#statements#&gt;</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>whileステートメントは次のように実行されます：</p><ol><li>条件が評価されます。</li><li>真であれば、実行はステップ2に進みます。偽であれば、プログラムはwhileステートメントの実行を終了します。</li><li>プログラムはステートメントを実行し、実行はステップ1に戻ります。</li></ol><p>条件の値は、ステートメントが実行される前に評価されるため、whileステートメント内のステートメントは0回以上実行される可能性があります。</p><p>条件の値は<code>Bool</code>型または<code>Bool</code>にブリッジされた型でなければなりません。条件は、Optional Bindingで説明されているように、オプショナルバインディング宣言であることもできます。</p><h4 id="whileステートメントの文法" tabindex="-1">Whileステートメントの文法 <a class="header-anchor" href="#whileステートメントの文法" aria-label="Permalink to &quot;Whileステートメントの文法&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>while-statement → while condition-list code-block</span></span>
<span class="line"><span>condition-list → condition | condition , condition-list</span></span>
<span class="line"><span>condition → expression | availability-condition | case-condition | optional-binding-condition</span></span>
<span class="line"><span>case-condition → case pattern initializer</span></span>
<span class="line"><span>optional-binding-condition → let pattern initializer? | var pattern initializer?</span></span></code></pre></div><h3 id="repeat-whileステートメント" tabindex="-1">Repeat-Whileステートメント <a class="header-anchor" href="#repeat-whileステートメント" aria-label="Permalink to &quot;Repeat-Whileステートメント&quot;">​</a></h3><p>repeat-whileステートメントは、条件が真である限り、1回以上コードのブロックを実行することを可能にします。</p><p>repeat-whileステートメントは次の形式を持ちます：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>repeat {</span></span>
<span class="line"><span>   &lt;#statements#&gt;</span></span>
<span class="line"><span>} while &lt;#condition#&gt;</span></span></code></pre></div><p>repeat-whileステートメントは次のように実行されます：</p><ol><li>プログラムはステートメントを実行し、実行はステップ2に進みます。</li><li>条件が評価されます。</li><li>真であれば、実行はステップ1に戻ります。偽であれば、プログラムはrepeat-whileステートメントの実行を終了します。</li></ol><p>条件の値は、ステートメントが実行された後に評価されるため、repeat-whileステートメント内のステートメントは少なくとも1回は実行されます。</p><p>条件の値は<code>Bool</code>型または<code>Bool</code>にブリッジされた型でなければなりません。</p><h4 id="repeat-whileステートメントの文法" tabindex="-1">Repeat-Whileステートメントの文法 <a class="header-anchor" href="#repeat-whileステートメントの文法" aria-label="Permalink to &quot;Repeat-Whileステートメントの文法&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>repeat-while-statement → repeat code-block while expression</span></span></code></pre></div><h2 id="分岐ステートメント" tabindex="-1">分岐ステートメント <a class="header-anchor" href="#分岐ステートメント" aria-label="Permalink to &quot;分岐ステートメント&quot;">​</a></h2><p>分岐ステートメントは、1つ以上の条件の値に応じて、特定のコード部分を実行することをプログラムに許可します。分岐ステートメントで指定された条件の値は、プログラムがどのように分岐するか、したがってどのコードブロックが実行されるかを制御します。Swiftには、ifステートメント、guardステートメント、およびswitchステートメントの3つの分岐ステートメントがあります。</p><p>ifステートメントまたはswitchステートメント内の制御フローは、breakステートメントによって変更されることがあり、以下のBreak Statementで説明されています。</p><h3 id="分岐ステートメントの文法" tabindex="-1">分岐ステートメントの文法 <a class="header-anchor" href="#分岐ステートメントの文法" aria-label="Permalink to &quot;分岐ステートメントの文法&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>branch-statement → if-statement</span></span>
<span class="line"><span>branch-statement → guard-statement</span></span>
<span class="line"><span>branch-statement → switch-statement</span></span></code></pre></div><h3 id="if文" tabindex="-1">If文 <a class="header-anchor" href="#if文" aria-label="Permalink to &quot;If文&quot;">​</a></h3><p>if文は、1つ以上の条件の評価に基づいてコードを実行するために使用されます。</p><p>if文には2つの基本的な形式があります。どちらの形式でも、開き中括弧と閉じ中括弧が必要です。</p><p>最初の形式では、条件が真の場合にのみコードを実行することができ、次の形式を持ちます。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>if &lt;#condition#&gt; {</span></span>
<span class="line"><span>   &lt;#statements#&gt;</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>if文の2番目の形式では、追加のelse句（elseキーワードで導入される）を提供し、条件が真の場合に1つのコード部分を実行し、同じ条件が偽の場合に別のコード部分を実行するために使用されます。単一のelse句が存在する場合、if文は次の形式を持ちます。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>if &lt;#condition#&gt; {</span></span>
<span class="line"><span>   &lt;#statements to execute if condition is true#&gt;</span></span>
<span class="line"><span>} else {</span></span>
<span class="line"><span>   &lt;#statements to execute if condition is false#&gt;</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>if文のelse句には、複数の条件をテストするために別のif文を含めることができます。このように連鎖したif文は次の形式を持ちます。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>if &lt;#condition 1#&gt; {</span></span>
<span class="line"><span>   &lt;#statements to execute if condition 1 is true#&gt;</span></span>
<span class="line"><span>} else if &lt;#condition 2#&gt; {</span></span>
<span class="line"><span>   &lt;#statements to execute if condition 2 is true#&gt;</span></span>
<span class="line"><span>} else {</span></span>
<span class="line"><span>   &lt;#statements to execute if both conditions are false#&gt;</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>if文の条件の値は、<code>Bool</code>型または<code>Bool</code>にブリッジされた型でなければなりません。条件は、オプショナルバインディング宣言であることもできます。オプショナルバインディングについては、オプショナルバインディングで説明します。</p><h4 id="if文の文法" tabindex="-1">if文の文法 <a class="header-anchor" href="#if文の文法" aria-label="Permalink to &quot;if文の文法&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>if-statement → if condition-list code-block else-clause?</span></span>
<span class="line"><span>else-clause → else code-block | else if-statement</span></span></code></pre></div><h3 id="guard文" tabindex="-1">Guard文 <a class="header-anchor" href="#guard文" aria-label="Permalink to &quot;Guard文&quot;">​</a></h3><p>guard文は、1つ以上の条件が満たされない場合にプログラムの制御をスコープ外に移すために使用されます。</p><p>guard文は次の形式を持ちます。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>guard &lt;#condition#&gt; else {</span></span>
<span class="line"><span>   &lt;#statements#&gt;</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>guard文の条件の値は、<code>Bool</code>型または<code>Bool</code>にブリッジされた型でなければなりません。条件は、オプショナルバインディング宣言であることもできます。オプショナルバインディングについては、オプショナルバインディングで説明します。</p><p>guard文の条件でオプショナルバインディング宣言から値を割り当てられた定数または変数は、guard文の囲むスコープの残りの部分で使用できます。</p><p>guard文のelse句は必須であり、<code>Never</code>戻り型の関数を呼び出すか、次のいずれかの文を使用してguard文の囲むスコープの外にプログラム制御を移さなければなりません。</p><ul><li><code>return</code></li><li><code>break</code></li><li><code>continue</code></li><li><code>throw</code></li></ul><p>制御転送文については、以下の制御転送文で説明します。<code>Never</code>戻り型の関数についての詳細は、戻らない関数を参照してください。</p><h4 id="guard文の文法" tabindex="-1">guard文の文法 <a class="header-anchor" href="#guard文の文法" aria-label="Permalink to &quot;guard文の文法&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>guard-statement → guard condition-list else code-block</span></span></code></pre></div><h3 id="switch文" tabindex="-1">Switch文 <a class="header-anchor" href="#switch文" aria-label="Permalink to &quot;Switch文&quot;">​</a></h3><p>switch文は、制御式の値に応じて特定のコードブロックを実行することを可能にします。</p><p>switch文は次の形式を持ちます。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>switch &lt;#control expression#&gt; {</span></span>
<span class="line"><span>case &lt;#pattern 1#&gt;:</span></span>
<span class="line"><span>    &lt;#statements#&gt;</span></span>
<span class="line"><span>case &lt;#pattern 2#&gt; where &lt;#condition#&gt;:</span></span>
<span class="line"><span>    &lt;#statements#&gt;</span></span>
<span class="line"><span>case &lt;#pattern 3#&gt; where &lt;#condition#&gt;,</span></span>
<span class="line"><span>    &lt;#pattern 4#&gt; where &lt;#condition#&gt;:</span></span>
<span class="line"><span>    &lt;#statements#&gt;</span></span>
<span class="line"><span>default:</span></span>
<span class="line"><span>    &lt;#statements#&gt;</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>switch文の制御式は評価され、各ケースで指定されたパターンと比較されます。一致が見つかると、プログラムはそのケースのスコープ内にリストされた文を実行します。各ケースのスコープは空であってはなりません。その結果、各ケースラベルのコロン（:）の後に少なくとも1つの文を含める必要があります。マッチしたケースの本体でコードを実行する意図がない場合は、単一のbreak文を使用してください。</p><p>コードが分岐できる式の値は非常に柔軟です。たとえば、整数や文字などのスカラー型の値に加えて、浮動小数点数、文字列、タプル、カスタムクラスのインスタンス、およびオプショナルの値にも分岐できます。制御式の値は、列挙型のケースの値に一致させたり、指定された範囲の値に含まれているかどうかを確認したりすることもできます。これらのさまざまな型の値をswitch文で使用する方法の例については、制御フローのswitchを参照してください。</p><p>switchケースには、各パターンの後にwhere句を含めることができます。where句はwhereキーワードに続く式で導入され、パターンが制御式に一致する前に追加の条件を提供するために使用されます。where句が存在する場合、関連するケース内の文は、制御式の値がケースのパターンの1つに一致し、where句の式が真と評価された場合にのみ実行されます。たとえば、次の例では、制御式が2つの同じ値を含むタプル（例：(1, 1)）である場合にのみ、ケースが一致します。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>case let (x, y) where x == y:</span></span></code></pre></div><p>上記の例が示すように、ケース内のパターンはletキーワードを使用して定数をバインドすることもできます（varキーワードを使用して変数をバインドすることもできます）。これらの定数（または変数）は、対応するwhere句およびケースのスコープ内の残りのコードで参照できます。ケースに制御式と一致する複数のパターンが含まれている場合、すべてのパターンは同じ定数または変数バインディングを含む必要があり、各バインドされた変数または定数はすべてのケースのパターンで同じ型でなければなりません。</p><p>switch文には、defaultキーワードで導入されるデフォルトケースを含めることもできます。デフォルトケース内のコードは、他のケースが制御式と一致しない場合にのみ実行されます。switch文には1つのデフォルトケースしか含めることができず、それはswitch文の最後に表示されなければなりません。</p><p>パターンマッチング操作の実際の実行順序、特にケース内のパターンの評価順序は指定されていませんが、switch文のパターンマッチングはソース順で評価されるかのように動作します。つまり、ソースコードに表示される順序です。その結果、複数のケースに同じ値に評価されるパターンが含まれている場合、プログラムはソース順で最初に一致するケース内のコードのみを実行します。</p><h4 id="スイッチ文は網羅的でなければならない" tabindex="-1">スイッチ文は網羅的でなければならない <a class="header-anchor" href="#スイッチ文は網羅的でなければならない" aria-label="Permalink to &quot;スイッチ文は網羅的でなければならない&quot;">​</a></h4><p>Swiftでは、制御式の型のすべての可能な値が少なくとも1つのケースのパターンに一致する必要があります。これが単に実現不可能な場合（例えば、制御式の型が<code>Int</code>の場合）、要件を満たすためにデフォルトケースを含めることができます。</p><h4 id="将来の列挙ケースに対するスイッチ" tabindex="-1">将来の列挙ケースに対するスイッチ <a class="header-anchor" href="#将来の列挙ケースに対するスイッチ" aria-label="Permalink to &quot;将来の列挙ケースに対するスイッチ&quot;">​</a></h4><p>非フローズン列挙は、アプリをコンパイルして出荷した後でも、新しい列挙ケースを追加できる特別な種類の列挙です。非フローズン列挙に対するスイッチは追加の考慮が必要です。ライブラリの作者が列挙を非フローズンとしてマークすると、新しい列挙ケースを追加する権利を留保し、その列挙とやり取りするコードは再コンパイルせずに将来のケースを処理できる必要があります。ライブラリ進化モードでコンパイルされたコード、Swift標準ライブラリのコード、AppleフレームワークのSwiftオーバーレイ、およびCやObjective-Cのコードは、非フローズン列挙を宣言できます。フローズンと非フローズンの列挙についての情報は、フローズンを参照してください。</p><p>非フローズン列挙値に対するスイッチでは、すべての列挙ケースに対応するスイッチケースがある場合でも、常にデフォルトケースを含める必要があります。デフォルトケースに<code>@unknown</code>属性を適用することができ、これはデフォルトケースが将来追加される列挙ケースのみに一致するべきであることを示します。Swiftは、デフォルトケースがコンパイラ時に既知の列挙ケースに一致する場合、警告を生成します。この将来の警告は、ライブラリの作者がスイッチケースに対応する新しいケースを列挙に追加したことを通知します。</p><p>次の例は、Swift標準ライブラリの<code>Mirror.AncestorRepresentation</code>列挙の既存の3つのケースすべてに対してスイッチを行います。将来追加のケースを追加した場合、コンパイラはスイッチ文を更新して新しいケースを考慮する必要があることを示す警告を生成します。</p><div class="language-swift vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">swift</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> representation: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Mirror</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">AncestorRepresentation</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> .</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">generated</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">switch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> representation {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">case</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> .</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">customized</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    print</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;最も近い祖先の実装を使用します。&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">case</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> .</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">generated</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    print</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;すべての祖先クラスのデフォルトミラーを生成します。&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">case</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> .</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">suppressed</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    print</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;すべての祖先クラスの表現を抑制します。&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">@unknown</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default:</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    print</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;このコードがコンパイルされたときに未知だった表現を使用します。&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &quot;すべての祖先クラスのデフォルトミラーを生成します。&quot;と表示されます。</span></span></code></pre></div><h4 id="実行は暗黙的にケースを通過しない" tabindex="-1">実行は暗黙的にケースを通過しない <a class="header-anchor" href="#実行は暗黙的にケースを通過しない" aria-label="Permalink to &quot;実行は暗黙的にケースを通過しない&quot;">​</a></h4><p>一致したケース内のコードの実行が終了すると、プログラムはスイッチ文から退出します。プログラムの実行は次のケースやデフォルトケースに続行しません。とはいえ、あるケースから次のケースに実行を続行したい場合は、<code>fallthrough</code>キーワードからなるfallthrough文をそのケースに明示的に含めます。fallthrough文の詳細については、以下のFallthrough Statementを参照してください。</p><h4 id="スイッチ文の文法" tabindex="-1">スイッチ文の文法 <a class="header-anchor" href="#スイッチ文の文法" aria-label="Permalink to &quot;スイッチ文の文法&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>switch-statement → switch expression { switch-cases? }</span></span>
<span class="line"><span>switch-cases → switch-case switch-cases?</span></span>
<span class="line"><span>switch-case → case-label statements</span></span>
<span class="line"><span>switch-case → default-label statements</span></span>
<span class="line"><span>switch-case → conditional-switch-case</span></span>
<span class="line"><span>case-label → attributes? case case-item-list :</span></span>
<span class="line"><span>case-item-list → pattern where-clause? | pattern where-clause? , case-item-list</span></span>
<span class="line"><span>default-label → attributes? default :</span></span>
<span class="line"><span>where-clause → where where-expression</span></span>
<span class="line"><span>where-expression → expression</span></span>
<span class="line"><span>conditional-switch-case → switch-if-directive-clause switch-elseif-directive-clauses? switch-else-directive-clause? endif-directive</span></span>
<span class="line"><span>switch-if-directive-clause → if-directive compilation-condition switch-cases?</span></span>
<span class="line"><span>switch-elseif-directive-clauses → elseif-directive-clause switch-elseif-directive-clauses?</span></span>
<span class="line"><span>switch-elseif-directive-clause → elseif-directive compilation-condition switch-cases?</span></span>
<span class="line"><span>switch-else-directive-clause → else-directive switch-cases?</span></span></code></pre></div><h2 id="ラベル付き文" tabindex="-1">ラベル付き文 <a class="header-anchor" href="#ラベル付き文" aria-label="Permalink to &quot;ラベル付き文&quot;">​</a></h2><p>ループ文、if文、スイッチ文、またはdo文の前に、ラベル名の後にコロン（:）を続けた文ラベルを付けることができます。文ラベルをbreak文やcontinue文と一緒に使用して、ループ文やスイッチ文で制御フローをどのように変更するかを明示的に示します。詳細は、以下のBreak StatementおよびContinue Statementを参照してください。</p><p>ラベル付き文のスコープは、文ラベルに続く文全体です。ラベル付き文をネストすることができますが、各文ラベルの名前は一意でなければなりません。</p><p>文ラベルの使用方法と例については、Control Flowのラベル付き文を参照してください。</p><h3 id="ラベル付き文の文法" tabindex="-1">ラベル付き文の文法 <a class="header-anchor" href="#ラベル付き文の文法" aria-label="Permalink to &quot;ラベル付き文の文法&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>labeled-statement → statement-label loop-statement</span></span>
<span class="line"><span>labeled-statement → statement-label if-statement</span></span>
<span class="line"><span>labeled-statement → statement-label switch-statement</span></span>
<span class="line"><span>labeled-statement → statement-label do-statement</span></span>
<span class="line"><span>statement-label → label-name :</span></span>
<span class="line"><span>label-name → identifier</span></span></code></pre></div><h2 id="制御転送文" tabindex="-1">制御転送文 <a class="header-anchor" href="#制御転送文" aria-label="Permalink to &quot;制御転送文&quot;">​</a></h2><p>制御転送文は、プログラム内のコードの実行順序を無条件に別のコードに転送することで変更できます。Swiftには5つの制御転送文があります：break文、continue文、fallthrough文、return文、およびthrow文です。</p><h3 id="制御転送文の文法" tabindex="-1">制御転送文の文法 <a class="header-anchor" href="#制御転送文の文法" aria-label="Permalink to &quot;制御転送文の文法&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>control-transfer-statement → break-statement</span></span>
<span class="line"><span>control-transfer-statement → continue-statement</span></span>
<span class="line"><span>control-transfer-statement → fallthrough-statement</span></span>
<span class="line"><span>control-transfer-statement → return-statement</span></span>
<span class="line"><span>control-transfer-statement → throw-statement</span></span></code></pre></div><h3 id="break文" tabindex="-1">Break文 <a class="header-anchor" href="#break文" aria-label="Permalink to &quot;Break文&quot;">​</a></h3><p>break文は、ループ、if文、またはスイッチ文の実行を終了します。break文はbreakキーワードのみで構成される場合もあれば、以下のようにbreakキーワードの後に文ラベルの名前が続く場合もあります。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>break</span></span>
<span class="line"><span>break &lt;#label name#&gt;</span></span></code></pre></div><p>break文の後に文ラベルの名前が続く場合、そのラベルが付けられたループ、if文、またはスイッチ文の実行を終了します。</p><p>break文の後に文ラベルの名前が続かない場合、そのbreak文が含まれるスイッチ文または最も内側のループ文の実行を終了します。ラベルのないbreak文を使用してif文から抜け出すことはできません。</p><p>いずれの場合も、プログラムの制御は、囲むループまたはスイッチ文の後の最初の行に転送されます（存在する場合）。</p><p>break文の使用例については、Control FlowのBreak and Labeled Statementsを参照してください。</p><h4 id="break文の文法" tabindex="-1">Break文の文法 <a class="header-anchor" href="#break文の文法" aria-label="Permalink to &quot;Break文の文法&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>break-statement → break label-name?</span></span></code></pre></div><h3 id="continue文" tabindex="-1">continue文 <a class="header-anchor" href="#continue文" aria-label="Permalink to &quot;continue文&quot;">​</a></h3><p>continue文は、ループ文の現在の反復処理を終了しますが、ループ文の実行を停止しません。continue文は、continueキーワードのみで構成される場合と、以下のようにcontinueキーワードの後に文ラベルの名前が続く場合があります。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>continue</span></span>
<span class="line"><span>continue &lt;#label name#&gt;</span></span></code></pre></div><p>continue文の後に文ラベルの名前が続く場合、そのラベルで指定されたループ文の現在の反復処理を終了します。</p><p>continue文の後に文ラベルの名前が続かない場合、それが発生した最も内側の囲んでいるループ文の現在の反復処理を終了します。</p><p>いずれの場合も、プログラム制御は囲んでいるループ文の条件に移ります。</p><p>for文では、continue文が実行された後でもインクリメント式は評価されます。これは、ループの本体の実行後にインクリメント式が評価されるためです。</p><p>continue文の使用例については、制御フローのContinueおよびLabeled Statementsを参照してください。</p><h4 id="continue文の文法" tabindex="-1">continue文の文法 <a class="header-anchor" href="#continue文の文法" aria-label="Permalink to &quot;continue文の文法&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>continue-statement → continue label-name?</span></span></code></pre></div><h3 id="fallthrough文" tabindex="-1">fallthrough文 <a class="header-anchor" href="#fallthrough文" aria-label="Permalink to &quot;fallthrough文&quot;">​</a></h3><p>fallthrough文はfallthroughキーワードで構成され、switch文のcaseブロック内でのみ発生します。fallthrough文は、switch文のあるcaseから次のcaseへのプログラム実行を継続させます。switch文の制御式の値とcaseラベルのパターンが一致しなくても、プログラム実行は次のcaseに進みます。</p><p>fallthrough文は、caseブロックの最後の文としてだけでなく、switch文の任意の場所に現れることができますが、最後のcaseブロックでは使用できません。また、値バインディングパターンを含むパターンのcaseブロックに制御を移すこともできません。</p><p>switch文でfallthrough文を使用する例については、制御フローのControl Transfer Statementsを参照してください。</p><h4 id="fallthrough文の文法" tabindex="-1">fallthrough文の文法 <a class="header-anchor" href="#fallthrough文の文法" aria-label="Permalink to &quot;fallthrough文の文法&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fallthrough-statement → fallthrough</span></span></code></pre></div><h3 id="return文" tabindex="-1">return文 <a class="header-anchor" href="#return文" aria-label="Permalink to &quot;return文&quot;">​</a></h3><p>return文は関数またはメソッド定義の本体内で発生し、プログラム実行を呼び出し元の関数またはメソッドに戻します。プログラム実行は、関数またはメソッド呼び出しの直後のポイントで続行されます。</p><p>return文は、returnキーワードのみで構成される場合と、以下のようにreturnキーワードの後に式が続く場合があります。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>return</span></span>
<span class="line"><span>return &lt;#expression#&gt;</span></span></code></pre></div><p>return文の後に式が続く場合、その式の値が呼び出し元の関数またはメソッドに返されます。式の値が関数またはメソッド宣言で宣言された戻り値の型と一致しない場合、式の値は戻り値の型に変換されてから呼び出し元の関数またはメソッドに返されます。</p><blockquote><p>注: Failable Initializersで説明されているように、return文の特別な形式（return nil）は、初期化の失敗を示すために失敗可能なイニシャライザで使用できます。</p></blockquote><p>return文の後に式が続かない場合、それは値を返さない関数またはメソッドからのみ戻るために使用できます（つまり、関数またはメソッドの戻り値の型がVoidまたは()の場合）。</p><h4 id="return文の文法" tabindex="-1">return文の文法 <a class="header-anchor" href="#return文の文法" aria-label="Permalink to &quot;return文の文法&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>return-statement → return expression?</span></span></code></pre></div><h3 id="throw文" tabindex="-1">throw文 <a class="header-anchor" href="#throw文" aria-label="Permalink to &quot;throw文&quot;">​</a></h3><p>throw文は、throwing関数またはメソッドの本体内、またはthrowsキーワードでマークされたクロージャ式の本体内で発生します。</p><p>throw文は、現在のスコープのプログラム実行を終了し、その囲んでいるスコープにエラー伝播を開始させます。スローされたエラーは、do文のcatch句で処理されるまで伝播し続けます。</p><p>throw文は、以下のようにthrowキーワードの後に式が続く形で構成されます。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>throw &lt;#expression#&gt;</span></span></code></pre></div><p>式の値はErrorプロトコルに準拠する型でなければなりません。throw文を含むdo文または関数がスローするエラーの型を宣言している場合、式の値はその型のインスタンスでなければなりません。</p><p>throw文の使用例については、エラーハンドリングのThrowing Functionsを参照してください。</p><h4 id="throw文の文法" tabindex="-1">throw文の文法 <a class="header-anchor" href="#throw文の文法" aria-label="Permalink to &quot;throw文の文法&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>throw-statement → throw expression</span></span></code></pre></div><h3 id="defer文" tabindex="-1">defer文 <a class="header-anchor" href="#defer文" aria-label="Permalink to &quot;defer文&quot;">​</a></h3><p>defer文は、defer文が現れるスコープの外にプログラム制御を移す直前にコードを実行するために使用されます。</p><p>defer文は次の形式を持ちます：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>defer {</span></span>
<span class="line"><span>    &lt;#statements#&gt;</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>defer文内の文は、プログラム制御がどのように移されても実行されます。これは、たとえばファイルディスクリプタを閉じるなどの手動リソース管理や、エラーがスローされた場合でも実行する必要があるアクションを実行するためにdefer文を使用できることを意味します。</p><p>defer文内の文は、defer文を囲むスコープの終わりに実行されます。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>func f(x: Int) {</span></span>
<span class="line"><span>  defer { print(&quot;First defer&quot;) }</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  if (x &lt; 10) {</span></span>
<span class="line"><span>    defer { print(&quot;Second defer&quot;) }</span></span>
<span class="line"><span>    print(&quot;End of if&quot;)</span></span>
<span class="line"><span>  }</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  print(&quot;End of function&quot;)</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span>f(x: 5)</span></span>
<span class="line"><span>// Prints &quot;End of if&quot;</span></span>
<span class="line"><span>// Prints &quot;Second defer&quot;</span></span>
<span class="line"><span>// Prints &quot;End of function&quot;</span></span>
<span class="line"><span>// Prints &quot;First defer&quot;</span></span></code></pre></div><p>上記のコードでは、if文内のdeferは関数f内で宣言されたdeferよりも先に実行されます。これは、if文のスコープが関数のスコープよりも先に終了するためです。</p><p>同じスコープ内に複数のdefer文が現れる場合、それらが現れる順序は実行される順序の逆になります。特定のスコープ内で最後のdefer文を最初に実行することは、その最後のdefer文内の文が他のdefer文によってクリーンアップされるリソースを参照できることを意味します。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>func f() {</span></span>
<span class="line"><span>    defer { print(&quot;First defer&quot;) }</span></span>
<span class="line"><span>    defer { print(&quot;Second defer&quot;) }</span></span>
<span class="line"><span>    print(&quot;End of function&quot;)</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span>f()</span></span>
<span class="line"><span>// Prints &quot;End of function&quot;</span></span>
<span class="line"><span>// Prints &quot;Second defer&quot;</span></span>
<span class="line"><span>// Prints &quot;First defer&quot;</span></span></code></pre></div><p>defer文内の文は、defer文の外にプログラム制御を移すことはできません。</p><h4 id="defer文の文法" tabindex="-1">defer文の文法 <a class="header-anchor" href="#defer文の文法" aria-label="Permalink to &quot;defer文の文法&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>defer-statement → defer code-block</span></span></code></pre></div><h3 id="do文" tabindex="-1">Do文 <a class="header-anchor" href="#do文" aria-label="Permalink to &quot;Do文&quot;">​</a></h3><p>do文は新しいスコープを導入するために使用され、オプションで1つ以上のcatch節を含むことができます。catch節には定義されたエラー条件に一致するパターンが含まれます。do文のスコープ内で宣言された変数や定数は、そのスコープ内でのみアクセスできます。</p><p>Swiftのdo文は、C言語でコードブロックを区切るために使用される中括弧({})に似ており、実行時にパフォーマンスコストは発生しません。</p><p>do文の形式は次の通りです：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>do {</span></span>
<span class="line"><span>    try &lt;#expression#&gt;</span></span>
<span class="line"><span>    &lt;#statements#&gt;</span></span>
<span class="line"><span>} catch &lt;#pattern 1#&gt; {</span></span>
<span class="line"><span>    &lt;#statements#&gt;</span></span>
<span class="line"><span>} catch &lt;#pattern 2#&gt; where &lt;#condition#&gt; {</span></span>
<span class="line"><span>    &lt;#statements#&gt;</span></span>
<span class="line"><span>} catch &lt;#pattern 3#&gt;, &lt;#pattern 4#&gt; where &lt;#condition#&gt; {</span></span>
<span class="line"><span>    &lt;#statements#&gt;</span></span>
<span class="line"><span>} catch {</span></span>
<span class="line"><span>    &lt;#statements#&gt;</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>do文は、スローするエラーの型をオプションで指定することができ、その形式は次の通りです：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>do throws(&lt;#type#&gt;) {</span></span>
<span class="line"><span>    try &lt;#expression#&gt;</span></span>
<span class="line"><span>} catch &lt;#pattern#&gt; {</span></span>
<span class="line"><span>    &lt;#statements#&gt;</span></span>
<span class="line"><span>} catch {</span></span>
<span class="line"><span>    &lt;#statements#&gt;</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>do文にthrows句が含まれている場合、doブロックは指定された型のエラーのみをスローできます。型はErrorプロトコルに準拠する具体的な型、Errorプロトコルに準拠する不透明な型、またはボックス化されたプロトコル型any Errorでなければなりません。do文がスローするエラーの型を指定しない場合、Swiftは次のようにエラーの型を推論します：</p><ul><li>doコードブロック内のすべてのthrows文とtry式が網羅的なエラーハンドリングメカニズム内にネストされている場合、Swiftはdo文がエラーをスローしないと推論します。</li><li>doコードブロックが網羅的なエラーハンドリングの外で単一の型のエラーのみをスローするコードを含む場合、Swiftはdo文がその具体的なエラー型をスローすると推論します。</li><li>doコードブロックが網羅的なエラーハンドリングの外で複数の型のエラーをスローするコードを含む場合、Swiftはdo文がany Errorをスローすると推論します。</li></ul><p>明示的な型を持つエラーの取り扱いについての詳細は、「エラーの型を指定する」を参照してください。</p><p>doコードブロック内の任意の文がエラーをスローした場合、プログラムの制御はそのエラーに一致する最初のcatch節に移ります。どの節も一致しない場合、エラーは周囲のスコープに伝播します。トップレベルでエラーが処理されない場合、プログラムの実行はランタイムエラーで停止します。</p><p>switch文のように、コンパイラはcatch節が網羅的かどうかを推論しようとします。そのような判断ができる場合、エラーは処理されたと見なされます。そうでない場合、エラーは含まれるスコープの外に伝播する可能性があり、その場合、エラーは囲むcatch節で処理されるか、含まれる関数がthrowsで宣言されている必要があります。</p><p>複数のパターンを持つcatch節は、そのパターンのいずれかがエラーに一致する場合、エラーに一致します。catch節に複数のパターンが含まれている場合、すべてのパターンは同じ定数または変数バインディングを含む必要があり、各バインドされた変数または定数はcatch節のすべてのパターンで同じ型を持つ必要があります。</p><p>エラーが処理されることを保証するために、ワイルドカードパターン(_)のようにすべてのエラーに一致するパターンを持つcatch節を使用します。catch節がパターンを指定しない場合、catch節は任意のエラーに一致し、エラーをerrorという名前のローカル定数にバインドします。catch節で使用できるパターンの詳細については、「パターン」を参照してください。</p><p>複数のcatch節を持つdo文の使用例については、「エラーの処理」を参照してください。</p><h4 id="do文の文法" tabindex="-1">do文の文法 <a class="header-anchor" href="#do文の文法" aria-label="Permalink to &quot;do文の文法&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>do-statement → do throws-clause? code-block catch-clauses?</span></span>
<span class="line"><span>catch-clauses → catch-clause catch-clauses?</span></span>
<span class="line"><span>catch-clause → catch catch-pattern-list? code-block</span></span>
<span class="line"><span>catch-pattern-list → catch-pattern | catch-pattern , catch-pattern-list</span></span>
<span class="line"><span>catch-pattern → pattern where-clause?</span></span></code></pre></div><h2 id="コンパイラ制御文" tabindex="-1">コンパイラ制御文 <a class="header-anchor" href="#コンパイラ制御文" aria-label="Permalink to &quot;コンパイラ制御文&quot;">​</a></h2><p>コンパイラ制御文は、プログラムがコンパイラの動作の側面を変更できるようにします。Swiftには3つのコンパイラ制御文があります：条件付きコンパイルブロック、行制御文、およびコンパイル時診断文です。</p><h3 id="コンパイラ制御文の文法" tabindex="-1">コンパイラ制御文の文法 <a class="header-anchor" href="#コンパイラ制御文の文法" aria-label="Permalink to &quot;コンパイラ制御文の文法&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>compiler-control-statement → conditional-compilation-block</span></span>
<span class="line"><span>compiler-control-statement → line-control-statement</span></span>
<span class="line"><span>compiler-control-statement → diagnostic-statement</span></span></code></pre></div><h3 id="条件付きコンパイルブロック" tabindex="-1">条件付きコンパイルブロック <a class="header-anchor" href="#条件付きコンパイルブロック" aria-label="Permalink to &quot;条件付きコンパイルブロック&quot;">​</a></h3><p>条件付きコンパイルブロックは、1つ以上のコンパイル条件の値に応じてコードを条件付きでコンパイルすることを可能にします。</p><p>すべての条件付きコンパイルブロックは、#if コンパイルディレクティブで始まり、#endif コンパイルディレクティブで終わります。単純な条件付きコンパイルブロックは次の形式を持ちます：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#if &lt;#コンパイル条件#&gt;</span></span>
<span class="line"><span>    &lt;#ステートメント#&gt;</span></span>
<span class="line"><span>#endif</span></span></code></pre></div><p>if 文の条件とは異なり、コンパイル条件はコンパイル時に評価されます。その結果、ステートメントはコンパイル条件がコンパイル時に true と評価された場合にのみコンパイルおよび実行されます。</p><p>コンパイル条件には、true および false のブールリテラル、-D コマンドラインフラグと共に使用される識別子、または以下の表に示すプラットフォーム条件のいずれかを含めることができます。</p><table tabindex="0"><thead><tr><th>プラットフォーム条件</th><th>有効な引数</th></tr></thead><tbody><tr><td>os()</td><td>macOS, iOS, watchOS, tvOS, visionOS, Linux, Windows</td></tr><tr><td>arch()</td><td>i386, x86_64, arm, arm64</td></tr><tr><td>swift()</td><td>&gt;= または &lt; の後にバージョン番号</td></tr><tr><td>compiler()</td><td>&gt;= または &lt; の後にバージョン番号</td></tr><tr><td>canImport()</td><td>モジュール名</td></tr><tr><td>targetEnvironment()</td><td>simulator, macCatalyst</td></tr></tbody></table><p>swift() および compiler() プラットフォーム条件のバージョン番号は、メジャー番号、オプションのマイナー番号、オプションのパッチ番号などで構成され、各部分はドット (.) で区切られます。比較演算子とバージョン番号の間に空白を入れてはいけません。compiler() のバージョンは、コンパイラのバージョンであり、コンパイラに渡される Swift バージョン設定に関係なくなります。swift() のバージョンは、現在コンパイルされている言語バージョンです。例えば、Swift 5 コンパイラを使用して Swift 4.2 モードでコードをコンパイルする場合、コンパイラのバージョンは 5 で、言語バージョンは 4.2 です。この設定で、次のコードはすべてのメッセージを出力します：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#if compiler(&gt;=5)</span></span>
<span class="line"><span>print(&quot;Swift 5 コンパイラ以降でコンパイルされました&quot;)</span></span>
<span class="line"><span>#endif</span></span>
<span class="line"><span>#if swift(&gt;=4.2)</span></span>
<span class="line"><span>print(&quot;Swift 4.2 モード以降でコンパイルされました&quot;)</span></span>
<span class="line"><span>#endif</span></span>
<span class="line"><span>#if compiler(&gt;=5) &amp;&amp; swift(&lt;5)</span></span>
<span class="line"><span>print(&quot;Swift 5 コンパイラ以降で、Swift 5 より前のモードでコンパイルされました&quot;)</span></span>
<span class="line"><span>#endif</span></span>
<span class="line"><span>// &quot;Swift 5 コンパイラ以降でコンパイルされました&quot; を出力</span></span>
<span class="line"><span>// &quot;Swift 4.2 モード以降でコンパイルされました&quot; を出力</span></span>
<span class="line"><span>// &quot;Swift 5 コンパイラ以降で、Swift 5 より前のモードでコンパイルされました&quot; を出力</span></span></code></pre></div><p>canImport() プラットフォーム条件の引数は、すべてのプラットフォームに存在しない可能性のあるモジュールの名前です。モジュール名にはピリオド (.) を含めることができます。この条件はモジュールをインポートできるかどうかをテストしますが、実際にインポートはしません。モジュールが存在する場合、プラットフォーム条件は true を返し、存在しない場合は false を返します。</p><p>targetEnvironment() プラットフォーム条件は、指定された環境用にコードがコンパイルされている場合に true を返し、それ以外の場合は false を返します。</p><blockquote><p>注: arch(arm) プラットフォーム条件は ARM 64 デバイスでは true を返しません。arch(i386) プラットフォーム条件は、コードが 32 ビット iOS シミュレータ用にコンパイルされる場合に true を返します。</p></blockquote><p>論理演算子 &amp;&amp;、||、および ! を使用してコンパイル条件を組み合わせたり否定したりし、グループ化のために括弧を使用することができます。これらの演算子は、通常のブール式を組み合わせるために使用される論理演算子と同じ結合性と優先順位を持ちます。</p><p>if 文と同様に、異なるコンパイル条件をテストするために複数の条件分岐を追加することができます。#elseif 節を使用して任意の数の追加分岐を追加できます。また、#else 節を使用して最後の追加分岐を追加することもできます。複数の分岐を含む条件付きコンパイルブロックは次の形式を持ちます：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#if &lt;#コンパイル条件 1#&gt;</span></span>
<span class="line"><span>    &lt;#コンパイル条件 1 が true の場合にコンパイルされるステートメント#&gt;</span></span>
<span class="line"><span>#elseif &lt;#コンパイル条件 2#&gt;</span></span>
<span class="line"><span>    &lt;#コンパイル条件 2 が true の場合にコンパイルされるステートメント#&gt;</span></span>
<span class="line"><span>#else</span></span>
<span class="line"><span>    &lt;#両方のコンパイル条件が false の場合にコンパイルされるステートメント#&gt;</span></span>
<span class="line"><span>#endif</span></span></code></pre></div><blockquote><p>注: 条件付きコンパイルブロックの本体内の各ステートメントは、コンパイルされない場合でも解析されます。ただし、コンパイル条件に swift() または compiler() プラットフォーム条件が含まれている場合は例外です：ステートメントは、プラットフォーム条件で指定された言語またはコンパイラのバージョンと一致する場合にのみ解析されます。この例外は、古いコンパイラが新しいバージョンの Swift で導入された構文を解析しようとしないようにするためのものです。</p></blockquote><p>明示的メンバー式を条件付きコンパイルブロックでラップする方法については、明示的メンバー式を参照してください。</p><h4 id="条件付きコンパイルブロックの文法" tabindex="-1">条件付きコンパイルブロックの文法 <a class="header-anchor" href="#条件付きコンパイルブロックの文法" aria-label="Permalink to &quot;条件付きコンパイルブロックの文法&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>conditional-compilation-block → if-directive-clause elseif-directive-clauses? else-directive-clause? endif-directive</span></span>
<span class="line"><span>if-directive-clause → if-directive compilation-condition statements?</span></span>
<span class="line"><span>elseif-directive-clauses → elseif-directive-clause elseif-directive-clauses?</span></span>
<span class="line"><span>elseif-directive-clause → elseif-directive compilation-condition statements?</span></span>
<span class="line"><span>else-directive-clause → else-directive statements?</span></span>
<span class="line"><span>if-directive → #if</span></span>
<span class="line"><span>elseif-directive → #elseif</span></span>
<span class="line"><span>else-directive → #else</span></span>
<span class="line"><span>endif-directive → #endif</span></span>
<span class="line"><span>compilation-condition → platform-condition</span></span>
<span class="line"><span>compilation-condition → identifier</span></span>
<span class="line"><span>compilation-condition → boolean-literal</span></span>
<span class="line"><span>compilation-condition → ( compilation-condition )</span></span>
<span class="line"><span>compilation-condition → ! compilation-condition</span></span>
<span class="line"><span>compilation-condition → compilation-condition &amp;&amp; compilation-condition</span></span>
<span class="line"><span>compilation-condition → compilation-condition || compilation-condition</span></span>
<span class="line"><span>platform-condition → os ( operating-system )</span></span>
<span class="line"><span>platform-condition → arch ( architecture )</span></span>
<span class="line"><span>platform-condition → swift ( &gt;= swift-version ) | swift ( &lt; swift-version )</span></span>
<span class="line"><span>platform-condition → compiler ( &gt;= swift-version ) | compiler ( &lt; swift-version )</span></span>
<span class="line"><span>platform-condition → canImport ( import-path )</span></span>
<span class="line"><span>platform-condition → targetEnvironment ( environment )</span></span>
<span class="line"><span>operating-system → macOS | iOS | watchOS | tvOS | visionOS | Linux | Windows</span></span>
<span class="line"><span>architecture → i386 | x86_64 | arm | arm64</span></span>
<span class="line"><span>swift-version → decimal-digits swift-version-continuation?</span></span>
<span class="line"><span>swift-version-continuation → . decimal-digits swift-version-continuation?</span></span>
<span class="line"><span>environment → simulator | macCatalyst</span></span></code></pre></div><h3 id="行制御文" tabindex="-1">行制御文 <a class="header-anchor" href="#行制御文" aria-label="Permalink to &quot;行制御文&quot;">​</a></h3><p>行制御文は、コンパイルされているソースコードの行番号やファイル名とは異なる行番号やファイル名を指定するために使用されます。行制御文を使用して、Swiftが診断およびデバッグの目的で使用するソースコードの場所を変更します。</p><p>行制御文には次の形式があります：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#sourceLocation(file: &lt;#file path#&gt;, line: &lt;#line number#&gt;)</span></span>
<span class="line"><span>#sourceLocation()</span></span></code></pre></div><p>行制御文の最初の形式は、行制御文に続くコード行から、#line、#file、#fileID、および#filePathリテラル式の値を変更します。行番号は#lineの値を変更し、ゼロより大きい任意の整数リテラルです。ファイルパスは#file、#fileID、および#filePathの値を変更し、文字列リテラルです。指定された文字列は#filePathの値になり、文字列の最後のパスコンポーネントが#fileIDの値として使用されます。#file、#fileID、および#filePathの詳細については、リテラル式を参照してください。</p><p>行制御文の2番目の形式である#sourceLocation()は、ソースコードの場所をデフォルトの行番号とファイルパスにリセットします。</p><h4 id="行制御文の文法" tabindex="-1">行制御文の文法 <a class="header-anchor" href="#行制御文の文法" aria-label="Permalink to &quot;行制御文の文法&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>line-control-statement → #sourceLocation ( file: file-path , line: line-number )</span></span>
<span class="line"><span>line-control-statement → #sourceLocation ( )</span></span>
<span class="line"><span>line-number → A decimal integer greater than zero</span></span>
<span class="line"><span>file-path → static-string-literal</span></span></code></pre></div><h3 id="コンパイル時診断文" tabindex="-1">コンパイル時診断文 <a class="header-anchor" href="#コンパイル時診断文" aria-label="Permalink to &quot;コンパイル時診断文&quot;">​</a></h3><p>Swift 5.9以前では、#warningおよび#error文はコンパイル中に診断を発行します。この動作は現在、Swift標準ライブラリのwarning(<em>:)およびerror(</em>:)マクロによって提供されています。</p><h2 id="利用可能条件" tabindex="-1">利用可能条件 <a class="header-anchor" href="#利用可能条件" aria-label="Permalink to &quot;利用可能条件&quot;">​</a></h2><p>利用可能条件は、指定されたプラットフォーム引数に基づいて、ランタイムでAPIの利用可能性を照会するために、if、while、およびguard文の条件として使用されます。</p><p>利用可能条件には次の形式があります：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>if #available(&lt;#platform name#&gt; &lt;#version#&gt;, &lt;#...#&gt;, *) {</span></span>
<span class="line"><span>    &lt;#statements to execute if the APIs are available#&gt;</span></span>
<span class="line"><span>} else {</span></span>
<span class="line"><span>    &lt;#fallback statements to execute if the APIs are unavailable#&gt;</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>利用可能条件を使用して、使用したいAPIがランタイムで利用可能かどうかに応じてコードブロックを実行します。コンパイラは、利用可能条件の情報を使用して、そのコードブロック内のAPIが利用可能であることを確認します。</p><p>利用可能条件は、プラットフォーム名とバージョンのカンマ区切りリストを取ります。プラットフォーム名にはiOS、macOS、watchOS、tvOS、およびvisionOSを使用し、対応するバージョン番号を含めます。*引数は必須であり、他のプラットフォームでは、利用可能条件によってガードされたコードブロックの本体がターゲットによって指定された最小デプロイメントターゲットで実行されることを指定します。</p><p>ブール条件とは異なり、&amp;&amp;や||のような論理演算子を使用して利用可能条件を組み合わせることはできません。!を使用して利用可能条件を否定する代わりに、次の形式の非利用可能条件を使用します：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>if #unavailable(&lt;#platform name#&gt; &lt;#version#&gt;, &lt;#...#&gt;) {</span></span>
<span class="line"><span>    &lt;#fallback statements to execute if the APIs are unavailable#&gt;</span></span>
<span class="line"><span>} else {</span></span>
<span class="line"><span>    &lt;#statements to execute if the APIs are available#&gt;</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>#unavailable形式は条件を否定するための構文糖です。非利用可能条件では、<em>引数は暗黙的であり、含めてはなりません。これは、利用可能条件の</em>引数と同じ意味を持ちます。</p><h4 id="利用可能条件の文法" tabindex="-1">利用可能条件の文法 <a class="header-anchor" href="#利用可能条件の文法" aria-label="Permalink to &quot;利用可能条件の文法&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>availability-condition → #available ( availability-arguments )</span></span>
<span class="line"><span>availability-condition → #unavailable ( availability-arguments )</span></span>
<span class="line"><span>availability-arguments → availability-argument | availability-argument , availability-arguments</span></span>
<span class="line"><span>availability-argument → platform-name platform-version</span></span>
<span class="line"><span>availability-argument → *</span></span>
<span class="line"><span>platform-name → iOS | iOSApplicationExtension</span></span>
<span class="line"><span>platform-name → macOS | macOSApplicationExtension</span></span>
<span class="line"><span>platform-name → macCatalyst | macCatalystApplicationExtension</span></span>
<span class="line"><span>platform-name → watchOS | watchOSApplicationExtension</span></span>
<span class="line"><span>platform-name → tvOS | tvOSApplicationExtension</span></span>
<span class="line"><span>platform-name → visionOS | visionOSApplicationExtension</span></span>
<span class="line"><span>platform-version → decimal-digits</span></span>
<span class="line"><span>platform-version → decimal-digits . decimal-digits</span></span>
<span class="line"><span>platform-version → decimal-digits . decimal-digits . decimal-digits</span></span></code></pre></div>`,215)]))}const g=s(i,[["render",p]]);export{u as __pageData,g as default};
